# ORIGIN.md — 实现来源声明

本文件用于记录本项目中每一项功能/语法特性的**实现依据**（通常来自公开的语言标准文档），
以证明本项目是**独立自研实现**，未复制或逐行移植 GCC（及其他编译器）的受版权保护源码。

> **为什么需要这份文档**
> 本项目以"逐步实现 GCC 所支持的语言特性"为目标，但**实现思路来自公开标准与通用编译器原理，而非 GCC 源码**。
> 这样选择 **MIT / Apache-2.0 等宽松许可证**才是合法且干净的。此文件即为此目的提供书面依据。

---

## 目录

- [实现来源原则](#实现来源原则)
- [特性清单模板](#特性清单模板)
- [标准文档参考表](#标准文档参考表)
- [TODO / 待补充来源](#todo--待补充来源)

---

## 实现来源原则

本项目遵循以下规则：

1. **只依据公开语言标准**（ISO/IEC 标准、公开草案、规范文档）与**通用编译器原理**进行实现。
2. **不阅读、不复制、不逐行移植** GCC 及其他编译器的源码。
3. 若需参考现有实现，**仅参考其公开文档/行为描述**，而非其受保护的源码，并在下方记录出处。
4. 每条实现完成后，在本文件登记：对应特性、参考文档、日期、实现者。
5. 本项目是**测试 DeepSeek-V4-Flash（大语言模型）对编译器理解水平的实验**：由该模型在
   实现前独立推导设计思路，对照验证**仅限**通过本地运行真实编译器观察其**行为**
   （观察输出、错误信息等，不阅读源码），结果登记于本文件。

   **关于"本地运行真实编译器"的获取方式界定**（下载官方源码包并在本地构建出可执行文件，
   属于获取"运行时行为观察工具"的正当途径，**不构成**原则 2 所禁止的"阅读/复制/移植源码"）：
   - 源码包与构建产物仅存于本地，**不进入本仓库**，也不与本项目代码混放；
   - 构建过程只执行 configure / make 等机械步骤；若因工具链过新导致构建失败，仅修复
     构建层兼容问题（configure、Makefile、依赖库版本等），**不打开编译器的设计性源码**；
   - 模型在实现本项目的任何阶段，都不阅读、不复制、不逐行移植上述源码；
   - 对照验证的依据**仅是运行时行为**（编译输出、错误信息、汇编产物等），不依据源码结构；
   - **源码存留**：构建完成后，源码树与源码包**不得留存于实验环境**（仅保留黑盒构建产物）；
     如后续需要重建，允许按需重新下载，且下载/重建行为必须在日志中登记留痕。
   - **阶段门控（对照开关）**：是否启用"本地运行真实编译器/与任何 C/C++ 编译器做行为对照"，
     由用户**按阶段显式开启**，默认关闭。**当前自研阶段**（自汇编至高级语言语法，全部自己实现）
     禁止运行任何 C/C++ 编译器作为对照或参考——对照即"参考答案"，会诱发 AI 以行为克隆方式
     越狱，破坏独立推导的完整性。故本阶段不调用 gcc295 等黑盒产物；仅用户后续显式放行的阶段方可对照。

---

## 特性清单模板

> 复制下面表格，每实现一个特性就追加一行。

| 版本/阶段 | 特性 / 语法 | 参考来源（标准/文档） | 来源类型 | 实现日期 | 备注 |
|-----------|-------------|------------------------|----------|----------|------|
| v0        | 词法分析（数字/算子/括号/空白）+ 整数四则表达式（优先级/左结合/一元负/括号）+ 无 libc 代码生成 | ISO/IEC 9899:1990 §6.1（词法元素）、§6.3（表达式：乘除优先于加减、关系等价、括号分组、一元减）；i386 `idivl` 商向零截断与 C89 整数除法一致（§6.3.5） | 标准 | 2026-08-25 | **无蛋重推完成**（手写汇编 stage-0）：`src/pggcc0.s`，构建链 `as --32 → ld -m elf_i386`（无 crt/libc），全链不调用任何 C/C++ 编译器；tests/run.sh 10/10 自证通过（详见 docs/logs/2026-08-25.md） |
| v1        | 变量声明与赋值：`int` 声明（`,` 分隔）、语句序列（`;` 结尾）、IDENT 标识符、`=` 右结合链赋值、符号表（≤32 变量、名 ≤16B）、栈帧局部变量槽、末语句值打印 | ISO/IEC 9899:1990 §6.1（词法元素：标识符/关键字）、§6.3（表达式：赋值即表达式、右结合）、§6.5（声明——统一声明符）、§6.6（语句——表达式语句） | 标准 | 2026-08-25 | **无蛋 stage-1**：`src/pggcc1.s`（由 pggcc0.s 扩展），输入改 stdin 程序文本；栈帧按运行时逐声明分配（细则 subl $N 的一次性预留改为逐声明 `subl $4,%esp`，语义等价，留痕见日志会话 5）；tests/run.sh 10/10 + 边界冒烟 18/18 |
| v2        | 函数定义与调用：`int` 函数定义（参数表，允许 `int` 类型前缀）、`return` 语句、函数调用（嵌套/链式、多参数）、cdecl 调用约定（实参从右到左压栈、调用方平衡）、`main` 入口固定并打印其返回值、函数级作用域 | ISO/IEC 9899:1990 §6.1（词法：关键字 return）、§6.3（函数调用表达式）、§6.7（函数定义——参数声明、返回语句）、§6.6（复合语句/return 语句） | 标准 | 2026-08-25 | **无蛋 stage-2**：`src/pggcc2.s`（由 pggcc1.s 扩展），文件头注明调用约定与栈帧；cdecl 实参从右到左压栈用"扫描实参边界 + 自右向左回放解析 + 栈式 SENTINEL 防嵌套"实现；tests/run.sh 11/11 + 边界冒烟 18/18 |
| v3        | 控制流与比较：`if`/`else`、`while`、`for`（三段 `opt := ε\|expr`，inc 在源码上先于 body、代码须在其后 → 沿用 v2"扫描+回放"发码）、比较 `== != < <= > >=`（结果 int 0/1）、真值（0 假非 0 真）、块作用域（`{}` 内声明入块生效出块失效、内层同名遮蔽外层、出块符号表弹回 + 发码 `addl $4k,%esp` 回收块内栈空间）、递归自证 `fact(5)==120` → **语言图灵完备（B0 硬门槛达成）** | ISO/IEC 9899:1990 §6.1（词法：关键字/运算符）、§6.2.1（作用域——块作用域/函数作用域）、§6.3.5（真值）、§6.3.8（关系运算符）、§6.3.9（相等运算符）、§6.6.3（复合语句）、§6.6.4（选择语句 if）、§6.6.5（迭代语句 while/for） | 标准 | 2026-08-25 | **无蛋 stage-3**：`src/pggcc3.s`（由 pggcc2.s 扩展）；比较发码 `setCC %al`+`movzbl` 无分支；语法/发码细节留痕见日志会话 8（cond 结束符、for 回放+peek/restore 恢复、块栈回收、函数体首层与参数同域）；tests/run.sh 15/15 + 边界冒烟 22/22 |
| v4        | 类型系统与核心子集（P4 一期／编译器核心子集 B1——生蛋原料）：`int`/`char` 类型、指针（层级 ≤2）与解引用、一维数组与下标、字符串/字符字面量（含转义与八进制 `\ddd`）、全局变量（.data/.bss 初值：标量/列表/字符串）、逻辑短路 `&& \|\| !`、`do-while`/`break`/`continue`、前置/后缀 `++ --`、复合赋值 `+= -= *= /=`、强转 `(int)(char)(int*)(char*)`、`void` 函数、注释 `/* */`、`char` 无符号零扩展、`print_str` 运行时助手 | ISO/IEC 9899:1990 §6.1（词法元素：字符/字符串字面量、转义序列、运算符）、§6.3（表达式：一元/强制类型转换/赋值运算）、§6.5（声明与初始化：全局初值）、§6.6（语句：do-while/break/continue）、§6.7（函数定义：void/char 返回） | 标准 | 2026-08-25 | **无蛋 stage-4**：`src/pggcc4.s`（由 pggcc3.s 扩展），生成程序首次含 `.rodata/.data/.bss` 数据段与字符串常量表（LstrN 带 NUL 终止）；符号表升级 {name,off,tbase,tptr,anum}；实现留痕 D1–D8 见文件头（赋值左值检测 is_assign_ahead、逐声明栈分配、返回指针、内建函数表、全局 tbase 内存槽 pd_tbase、6+1 处自证调试修复、子集限制）；tests/run.sh **57/57** + 边界冒烟 17/17（详见 docs/logs/2026-08-25.md 会话 10）；**B1 达成，触发 B0 决策点（B2 自举重写方案待用户确认）** |
| v4.1     | B2-P0 模板扩展（自举前置）：内建 `print_int`（十进制无换行）/`print_err`（fd2＋换行）/`exit`（退出码）；保留全局 `src_buf`/`src_len` → `_start` 预读 stdin（`read` 长度取声明 anum，读失败 src_len=0）；保留全局 `pg_quiet`（非 0 跳过 return 值打印）；头部/尾部发码移至解析后按 gs_* 条件化 | plan §4.4 §7（接口协议对齐）；v0–v4 既有裸 syscall 运行时模板自承（通用编译器原理）；无蛋构建链 as+ld（无 crt/libc） | 标准+自研 | 2026-08-25 | 随 B2 自举重写方案确认实施（architecture-b2-bootstrap §3）：tests/run.sh **72/72**（原 57 + 新增 12 + **D15 回归 3**：字符串实参含逗号不再误判为实参分隔符）；留痕 D9–D15/E1 见 pggcc4.s 文件头——含修复 v4 遗留死角 `.Lpg_long` int 标量初值 `.long 残值`（=0 变 32，D14）与 `out_line` 溢出加固（D13）、实参边界扫描引号/注释跳过（D15，B2a 自举发现）、源码读入上限 4096→65536（E1，B2 前置） |
| B2a      | **自举第一步（boot0 骨架）**：pggcc 语言书写的自举编译器 `src/boot0.pgc`（P0 面：读入 stdin + 词法 + 扁平算子栈表达式解析 + int32 钳制 + 发码 i386 AT&T .s，v0 模板自承；诊断对齐 §7 接口） | ISO/IEC 9899:1990 §6.3（表达式）/§6.1（词法）；v0 模板（本项目自承）；通用编译原理（算子栈/shunting-yard） | 标准+自研 | 2026-08-25 | **无蛋自举支线第一步**：tests/run_boot.sh **20/20**（v0 用例集 10/10 + 错误/边界 8 + 冒烟 2）；全链 as+ld 无任何 C/C++ 编译器；留痕：v4 先定义后调用禁互递归 → 表达式解析改扁平算子栈；引擎限制：字符串字面量 ≤255B、源码 ≤65536B；详见日志会话 13 |
| B2b      | **自举第二步（boot0 变量/赋值/符号表）**：pggcc 语言书写的自举编译器 `src/boot0.pgc`（P1 面：`int` 声明（`,` 分隔、多变量）、IDENT 变量（≤32、名 ≤16B、超长/重名/表满/未声明均 exit 2）、右结合链赋值、四则表达式、符号表＝扁平 char 缓冲 sn + 并行 int 偏移 so（无 struct）、声明/表达式语句、末语句值打印（声明语句结尾印 0） | ISO/IEC 9899:1990 §6.1（词法元素：标识符/关键字）、§6.3（表达式：赋值即表达式、右结合与链式）、§6.5（声明——统一声明符）、§6.6（语句——表达式语句）；v0/v1 模板与 §7 协议（本项目自承）；通用编译原理 | 标准+自研 | 2026-08-25 | **无蛋自举支线第二步**：bin0 = pggcc4 编译 boot0.pgc 复跑 v1 用例集：tests/run_boot.sh **39/39**（v0 继承 12 + v0 错误继承 5（协议演进：空程序 v0=exit2→v1=打印 0、'a' v0=非法字符 1→v1=未声明 2）+ v1 主用例 7 + v1 错误 5 + 边界冒烟 10）；run.sh **72/72** 回归不变；全链 as+ld 无任何 C/C++ 编译器；**登记 3 项 pggcc4 侧缺陷/待深修**（详见日志会话 14）：① 主循环体块内含 if → pggcc4 错发 `addl $16` 出块回收（local_bytes 记账错乱）→ boot0 主驱动重构为一层函数 one()/go()（循环体只留函数调用）规避；② pggcc4 对 ≥3 项 `\|\|` 链 codegen 首项恒假（最小复现 t1）→ 词法标识符判定改用嵌套 if 逐级判定；③ 符号表查找哨兵误判：变量槽偏移恒为负（-4k），`s<0` 判断会把命中当未命中 → 改 `s==-1`。另 self-check 教训：pd() 初版漏写「拷名入符号表」登记循环，fi 永不命中 |
| B2c      | **自举第三步（boot0 函数/调用/return）**：pggcc 语言书写的自举编译器 `src/boot0.pgc`（P2 面：函数定义（`int` 名（参数表，可选 `int` 前缀）`{` 语句 `}`）、参数（cdecl 槽 +8+4k(%ebp)）与局部（-4(l+1)(%ebp)，函数级作用域逐函数重置）、`return` 语句（popl %eax + 跳函数尾声 .LrN）、函数调用（实参顶层逗号扫描 + 自右向左回放解析 + `call f<idx>` + `addl $4n` + `pushl %eax`，嵌套调用经 ast 栈层叠保存 argp/endp、ar 深度计数）、函数表 fns[16]（重定义/未定义/缺 main 均 exit 2）、main 返回值打印（_start call f_main）） | ISO/IEC 9899:1990 §6.1（词法：关键字 return）、§6.3（函数调用表达式）、§6.7（函数定义——参数声明）、§6.6（return 语句）；v0–v2 模板与 §7 协议自承；通用编译器原理（cdecl 惯例） | 标准+自研 | 2026-08-25 | **无蛋自举支线第三步**：tests/run_boot.sh **29/29**（v0 继承 6（四则包进 main return）+ v1 继承 3（声明/赋值包进 main）+ v2 主用例 9（返回/单参/嵌套 `g(g(3))`/多参/局部+参数/参数改写/无参/负值）+ 边界冒烟 6（三链嵌套/多函数互调/参数·局部同名遮蔽/负参数/空函数体）+ 错误 5（缺 main/未定义/重定义/缺括号/缺分号））；run.sh **72/72** 回归不变；全链 as+ld 无任何 C/C++ 编译器；实现留痕（日志会话 15）：① 表达式解析器改为自递归 e_expr（函数调用实参=表达式=自递归调用，破 p_a↔p_e 互递归死结）；② 函数标签用索引 `f<idx>`（避免运行时动态名字输出）；③ 嵌套调用实参/结束位经全局 ast 栈层叠保存恢复（argp/endp）；④ 实参结束符 `)` 由 ar 深度计数 break（不误触多余右括号）；⑤ 每语句重置 aw/opn（fbody）。D16 延续规避：ptop/主循环体只留函数调用（ptop_one 分层） |
| B2d      | **自举第四步（boot0 控制流/比较/递归）**：pggcc 语言书写的自举编译器 `src/boot0.pgc`（P3 一期面：比较算子 `== != < <= > >=`（结果 int 0/1，x86 `setCC %al`+`movzbl` 无分支发码）、关键字 `if/else/while/for`、`if`/`if-else`/`else-if` 链、`while`、`for`（三段 `opt := ε\|expr`，init/cond/inc 均可空；inc 在源码上位于 body 之前、发码须在 body 之后 → 沿用 v2"扫描+回放"：incf 空增量标记 + body token 态（bpos/btk/btp/btl）保存恢复）、语句解析器改自递归 p_s（块 `{}`/控制流内联于同一函数，无互递归）、跳转标号体系 L%d（lc 计数器）、递归自证 `fact(5)==120`/`fact(10)==3628800`/`fib(7)==13` | ISO/IEC 9899:1990 §6.1（词法：关键字与运算符）、§6.3.8（关系运算符）、§6.3.9（相等运算符）、§6.4（常量/真值——非 0 即真）、§6.6.3（复合语句）、§6.6.4（选择语句 if）、§6.6.5（迭代语句 while/for）；v0–v2 模板与 §7 协议及"扫描+回放"机制自承 | 标准+自研 | 2026-08-25 | **无蛋自举支线第四步**：tests/run_boot.sh **54/54**（v0 继承 6 + v1 继承 3 + v2 主用例 9 + v2 边界冒烟 6 + v3/B2d 比较 8 + if/else·else-if 5 + while 3 + for（含空增量/嵌套）4 + 递归 3 + v2 错误 5 + v3/B2d 错误 2（缺括号 exit 2／`&&` 超面 exit 1））；run.sh **72/72** 回归不变；全链 as+ld 无任何 C/C++ 编译器；实现留痕（日志会话 16）：① 比较发码 setCC 无分支、算子栈 e_expr 的 iscm/pr 分层（比较 pr=0 最弱、左结合 pop 阈值）—与 v3 同构；② for 空增量 incf + body token 态恢复（防回放把 body 起点吃掉）；③ ns 分号严格性标志：if/while/for/块不要求分号，return/表达式/声明语句严格检查（恢复 B2c 缺分号 exit 2 语义，修复 B2d 重构初版误放开 `return 1 }` 漏检）；④ p_s 补声明 incf/bpos/btk/btp/btl（复跑初版 pggcc4 编译报 `error 11098 incf`，变量未声明即错误，符合子集语义）；⑤ pggcc4 侧引擎上限升级：MAX_STR 64→256（E2）、MAX_FUNC 32→64（E3） |
| B2e      | **自举第五步（boot0 块作用域）**：pggcc 语言书写的自举编译器 `src/boot0.pgc`（P3 一期面 + 块作用域：内层块声明遮蔽外层同名（fi 反向查找最内层获胜）、出块符号表弹回 sc/locn + 发码 `addl $4k,%esp` 回收块内栈空间、块后新声明复用已回收槽位、逐函数 `sc=0` 重置（跨函数残留清零）、函数体顶层与参数同函数级作用域（顶层重声明参数 exit 2；内层块同名参数合法遮蔽））；叠加 B2d 面（比较/if-else/while/for/递归）不变 | ISO/IEC 9899:1990 §6.1.2.1（作用域：块作用域——嵌套块可声明并隐藏外层同名标识符；函数体顶层与参数同函数作用域）；§6.3.2.3（标识符代表的对象存活期/销毁）；v3（pggcc3.s）块回收语义（addl $4k + 符号表弹回）与 §7 协议自承 | 标准+自研 | 2026-08-25 | **无蛋自举支线第五步**：tests/run_boot.sh **62/62**（B2d 54 全继承 + B2e 主用例 6：块内遮蔽外层返回 1／块内读写遮蔽 `a=a+5` 返 6／双重嵌套遮蔽返 3／循环块作用域不可见返 0／块内多声明 `b*10+c` 返 12／块后新声明+外层仍可见+槽位复用返 4 + B2e 错误 2：同块重声明、参数·函数体顶层同名均 exit 2）；run.sh **72/72** 回归不变；全链 as+ld 无任何 C/C++ 编译器；实现留痕（日志会话 17）：① fi() 反向查找（此前正向命中最外层同名，遮蔽必然失败）；② 全局块基栈 bstk[16]/blok[16]+bd（sc/locn 弹回点）；③ pd() 判重由全表 fi 改同作用域扫描 `[bstk[bd],sc)`（外层同名不再误判冲突）；④ `bstk[0]=0` 使参体同域（初稿 bstk[0]=sc 漏报 `int f(int a){int a;}`）；⑤ 逐函数 sc=0；.s 人工抽检槽位复用（b 于块内 -8、块出口 addl $4、c 复用 -8） |
| B2f      | **自举第六步（boot0 循环控制 do-while/break/continue；B2 支线收口）**：pggcc 语言书写的自举编译器 `src/boot0.pgc`（P3 一期面 + 循环控制：`do` 语句（先执行一次后判条件、尾 `;` 必需——ns 严格）、`break`/`continue`（**循环控制标号栈 lcs[16]/lbl[16]+ 深度 ld**：while {cont=t1,brk=t1+1}、for {cont=t1+1（增量段）,brk=t1+2}、do {cont=t1,brk=t1+1}；循环外使用 exit 2））；至此 B2 支线 B2a–f 全部落地（骨架→变量→函数→控制流+递归→块作用域→循环控制），boot0 覆盖 v3 面**全部语句构造** | ISO/IEC 9899:1990 §6.6.5（迭代语句：do-while 先执行后判断）、§6.6.6（跳转语句：break/continue 作用于最内层循环）；v3（pggcc3.s）循环控制语义自承 | 标准+自研 | 2026-08-25 | **无蛋自举支线第六步（收口）**：tests/run_boot.sh **73/73**（B2d/e 65 全继承 + B2f 主用例 8：do-while 求和 10／do 至少执行一次 1／while(1)+break 3／for+break 6／for+continue 8／while+continue 12／do+continue 8／嵌套 for 内层 break 6 + B2f 错误 3：循环外 break、循环外 continue、do-while 缺分号均 exit 2）；run.sh **72/72** 回归不变；全链 as+ld 无任何 C/C++ 编译器；实现留痕（日志会话 18）：① 循环标号栈 lcs/lbl（break→lbl[ld-1] 出口、continue→lcs[ld-1] 续点）；② **do 条件前补 aw=1/opn=0 复位**（do 语句体执行于条件前——body 的 p_s 会把 aw 清 0；while/if 条件在体前无此问题）；③ **for 的 continue 目标 = 增量段 L(t1+1)**（初版误指条件 L t1 → 增量被跳 → 死循环，被 timeout 暴露）；④ em_jnz 助手；⑤ 新关键字 do/break/continue 词法；**E4 引擎上限：MAX_GLB 64→128**（boot0 全局符号恰满 64，居尾的 astp 触发 reg_global `.Lrg_full` 误报语法错误——gdb 断 reg_global 逐全局定位；另记：初试 sed 改 MAX_GLB 缩进不匹配未生效致 128 假阴性，教训：改 .equ 须精确匹配） |
| v4.1+boot0 | **自举第七步收尾——B2g 自面补全一期推进至自举成功（bin0 编译 boot0.pgc 自身并产出可用编译器）**：在 v4.1/B2g 面基础上，本号段完成自编译成功与四项根因修复（详见日志会话 19 续）：① **D16 根治 pggcc4 侧缺陷**——parse_block 退出时 `blk_mbytes` 槽位读取错位（未 `decl blk_depth` 就读），导致局部栈记账错乱、块出口生成 spurious `addl $N,%esp`（N 正负皆有，随进入分支路径污染栈）；修复后 boot0.s 全无 spurious addl，多变量全局链/嵌套 if 全通；② **符号名登记补空终止符**（sn/fns/gan 三表），消除"短名覆盖长名槽位残留"（1 字符参数侥幸过、2 字符参数必崩——fi/fnfind/gfd 的 `sn[i*16+tl]==0` 哨兵被残留字符破坏）；③ **函数调用实参扫描跳过字符串/字符字面量**（字符串内逗号/括号不再误判参数分隔符与括号深度，`print_str("a, b")` 等模板字符串实参正确）；④ **表达式优先级体系统一为 C 风格分层 + 左结合弹栈**（pr：UN/NOT 4 > STAR/SLASH 3 > PLUS/MINUS 2 > 比较 1 > && 0 > \|\| -1；四处运算符弹栈判据统一 `pr(oq[opn-1])>=pr(tk)`）——修复 `c>='0'&&c<='9'` 类无括号组合在自编译产物中被拆成 `A&c` 独立项的关键退化；另：源缓冲 4096→65536（自编译源码长度）、p_a 全局数组下标左值/右值双路径（`x=ia[0]+ia[1]` 读路径）、p_k 裸 `return;` 路径（void 函数）。**自举验证链**：bin0（pggcc4 编译 boot0.pgc）→ **bin0 编译 boot0.pgc 自身成功（rc=0，产出 258KB .s/125 字符串）** → bin0b（自举产物）8 项行为与 bin0 完全一致 → bin0b 二次编译 boot0.pgc 成功（回稳）→ bin0c（二次自举产物）冒烟 `return 42`→42 ✅；tests/run_boot.sh **90/90** + run.sh **72/72** 回归不变；全链 as+ld 无任何 C/C++ 编译器、无源码阅读 | 同上（v4.1+B2g 标准+自研）；R2（差异归因）延续 | 2026-08-25 | **无蛋自举成功（B3 阶段条件达成）**：boot0.pgc 全体源码被自身编译产物递归编译成功且行为一致；详见 docs/logs/2026-08-25.md 会话 19 续 |
| B4       | **煮蛋闭台验证（architecture-b2-bootstrap §8 门控项）**：链＝pggcc4 直出 `boot0.s`→bin0 → bin0 编译 boot0.pgc→`boot0b.s`→bin1 → bin1 编译 boot0.pgc→`boot0c.s`→bin2；门1 `boot0b.s==boot0c.s`（259267B 逐字节一致，固定点·源码态）；门2 `cmp bin1 bin2` 二进制逐字节一致（编连固定点——首轮 FAIL 系 **ld 在 .symtab 嵌入输入 .o 文件名符号**（boot0b.o/boot0c.o）之 ELF 符号噪声，非机器码差异，同名 .o 重链消除后逐字节一致）；门3 跨链比对──pggcc4 直出与 boot0 直出同源码，**不同实现文本结构性差异**（358352 vs 259267B）非逐字节可比，跨链语义等价由行为矩阵＋双链回归覆盖；门4 三头行为矩阵 24/24（bin0/bin1/bin2 同输入同输出）；门5 pg_quiet 语义三头 0 字节 3/3。**整改项（boot0 侧缺陷）**：`eh()` 模板 `pg_quiet` 只门控 `call print_decimal`，尾部换行（`movl $10,%eax; call print_char`）恒发——pg_quiet=1 目标多 1B `\n`（pggcc4 链 0 字节），违反 v4.1「跳过返回值的打印与换行」语义；修复为换行随 print_decimal 同条件发射（boot0.pgc 文件头 B4 find 留痕） | 无任何编译器对照；期望值手算；ISO/IEC 9899:1990 语义口径（同前）；自研 | 标准+自研 | 2026-08-26 | **B4 煮蛋闭台验证通过（用户放行执行）**：门1/门2/门4/门5 全过、门3 异型已释（跨链语义等价）；门脚本 `tests/boot_b4.sh` 入库；run.sh **72/72** + run_boot.sh **90/90** 回归不变；全链 as+ld 无任何 C/C++ 编译器、无源码阅读；详见 docs/logs/2026-08-26.md 会话 1 |
| B5-1     | **B5 首轮链上面扩张（演示轮）：`char*`/`int*` 形参**——boot0 增：①全局 `so_t[32]/so_p[32]` 形参类型表（pfun 解析 `int`/`char` 前缀＋可选 `*`，so_t=0/1 基型、so_p=0/1 指针层；pd() 局部登记 0/0）；②指针形参下标读/写（e_expr 读：`popl %ecx/%eax`→char `movzbl (%eax,%ecx)`、int `imull $4/addl`；p_a 写：`popl v/idx/base`→char `movb %al,(%edx,%ecx)`、int 缩放后 `movl %eax,(%edx)`）；③自用 `void wpf(char *s, int v){ print_str(s); print_int(v); }` 替换 pfun `f%d:`/pgbss·pgvk `g%d`/sbro `str%d:` 标签发码（字符串字面量实参路径自证）。**新增 `fi_i()`**（返回符号索引）——fi() 返回 `so[i]` 槽偏移，新下标路径按索引访问 so_t/so_p 时必须用 fi_i。**E5：pggcc4 MAX_STR 256→512**（136 唯一字面量×peek/回放重复词法 ~1.9×＝实入 257 次 >256，gdb 断 str_count 检查实证） | 无任何编译器对照；手算期望值；ISO/IEC 9899:1990 §6.3（指针/下标）语义口径；自研（§8.1 面扩张原则） | 标准+自研 | 2026-08-26 | **B5 首轮面扩张完成**：run_boot.sh **100/100**（原 90＋新 10：串长/转发/全局数组指针求和/双指针/指针写/复合）；boot_b4.sh 闭台复验全门 **PASS**（新面下固定点不破）；run.sh **72/72**；全链 as+ld 无任何 C/C++ 编译器、无源码阅读；详见 docs/logs/2026-08-26.md 会话 3 |
| P4-II（T1–T5） | **P4 二期分片一（链上，C90 全量推进）**——T1 `const`/`volatile` 吸收＋指针声明符（多级 `*`、`*`+`[N]`，pd 改带基型、pdlv/gptr）；T2 三目 `?:`（tn/tbd/tbs 分支基深；分支保留 opn，RPAREN/尾部抽干容忍外层括号）＋逗号表达式（ar==0 或括号内）＋`sizeof`（类型/标识符/全局数组）；T3 位运算 `& \| ^ ~ << >>`＋`%` 取模（C 全序 pr 5..-4，idivl %edx 余，shll/shrl %cl）；T4 `switch/case/default`（预扫描 case 常量→比较链→按序 case 体 fallthrough；每 switch 临时槽；break 走 lcs/lbl；swd 深度化扁平 swcv/swlb；case 支持 enum 常量）；T5 `enum` 常量（csn/csv+cstfind）与 `typedef` 别名（tdn/tdb/tdp+tdfind，接线 pd/pfun/pgv/ptop_one）。**引擎上限族**：E6 pggcc4 MAX_GLB 128→256（boot0 全局 131，T_* 常量行 56 个命名常量计全局）；E6b boot0 自编译表 gst/gn/gptr→256、gan→4096 | 无任何编译器对照；期望值手算；ISO/IEC 9899:1990 §6.3.5/.7/.15/.16（表达式）、§6.5.2.2（枚举）、§6.5.5/§6.7（typedef/声明）、§6.6.4.2（switch）；自研 | 标准+自研 | 2026-08-26 | **P4-II T1–T5 完成（链上）**：run_boot.sh **108→122→139→150→160 逐片全绿不回归**；boot_b4 闭台每片全门 **PASS**（固定点不破）；run.sh **72/72**；全链 as+ld 无任何 C/C++ 编译器、无源码阅读；详见 docs/logs/2026-08-26.md 会话 4。**剩余 T6 struct/union、T7 多维数组＋完整复合声明符语义——已圈定，次轮接续** |
| P4-II（T6/T7） | **P4 二期分片二（链上）——T6 struct/union**：类型表（stt/stnm/stsz/stu/smc/smn/smo/smt/sms）；pstm 成员布局（int 4 字节对齐、char=1、union 取最大、嵌套 struct 成员 smt=2+sms 递归求址）；`.` 点访问读/写（e_expr/p_a，支持 `s.a.b` 链偏移累加、末尾成员 char/int 定 movzbl/movl）；struct 类型全局变量（pgsid→gstt 记 struct id、pgst 按 stsz `.space`）与局部变量（p_s 分支、so_st=sid+1、按 ceil(stsz/4) 分配栈槽）；整值赋值 `s2=s1`（ecpy 编译期展开 movl/movb 逐段拷贝，局部/全局，链式赋值值=末段）；`sizeof(struct/union)`（类型名/变量）。**T7 局部一/二维数组**：pd 增 `[N][M]` 后缀（so_sz=总元素、so_p=列数、基址=最低栈槽）；i386 寻址模式 `so(%ebp,%ecx[,4])` 直接读写（char esz=1/int esz=4）；数组名退化 leal、作为实参传递；sizeof(局部数组)；承接全局二维（gcol）。**引擎上限族**：E7 pggcc4 MAX_STR 512→1024（boot0 print_str 调用 165→265、wpf 峰值 ~1.9× 重登记冲过 512）、boot0 自表 stp0→1024/sts→32768/`stc>=1024`（自编译闭环） | 无任何编译器对照；期望值手算；ISO/IEC 9899:1990 §6.5.2.1/.2.2/.3.3/.5（结构体/联合体/成员/整值拷贝）、§6.3.3（sizeof）、§6.5.2.1（数组下标）、§6.5.6（数组名退化）；自研（§8.1 面扩张原则） | 标准+自研 | 2026-08-26 | **P4-II T6/T7 完成（链上）**：run_boot.sh **167→189 全绿不回归**（T6 12 例：全局/局部点读写、char 成员对齐、嵌套 `r.p.x`、整值赋值局部+全局、sizeof、union 布局；T7 局部数组 9 例：1D/2D/char/函数传参/sizeof/指针混用；错误 1）；boot_b4 闭台**全门 PASS**（固定点不破）；全链 as+ld 无任何 C/C++ 编译器、无源码阅读；**通用约束登记：标点 token（`.` 等）后 `tl` 被污染为 1、nm 保留旧名——凡标点后做名查找必须恢复 `tl`**；详见 docs/logs/2026-08-26.md 会话 5。剩余已圈定限制：匿名嵌套 struct 定义、struct 数组/参数/初始化、`&a[i]`/`&s.m`、声明符括号（int (\*p)[3]）、三维数组、`->` |
| P4-II（余项） | **P4 二期分片三（链上，剩余限制清零）**——① `->` 运算符：T_ARROW token＋词法（`-`+`>`）；e_expr/p_a 读/写分支（编译期成员链偏移＋运行时指针值装载 movl，si 局部/全局均支持，写路径 `p->m=v`）；② 匿名嵌套 struct/union 定义：pstm 遇 `{` 递归建临时类型（stt 增长、stnm 空名）、外层登记 smt=2+sms 链；③ struct 数组/初始化：局部/全局数组（so_sz＋so_st / gstt＋gn），下标×stsz 寻址、`a[i].m` 链读/写（四个数组分支栈式扩展）；初始化器 pginit 递归平铺（dty：0=int/1=char/2+=struct，任意维嵌套 `{}` 展开、字段按 smt 出 .long/.byte）；`struct P g={..}` 单值初值与 `a[N]={..}` 均 .data 直排；struct 参数（pfun 分支 so_par=1 引用传递＋so_pst 记 struct id，调用侧自动取址 leal，函数体 `s.m` movl 装载）；④ `&a[i]`/`&s.m`：T_BAND 取址分支重构（1D/2D 下标×esz、`.` 成员链 leal＋off、下标递归 opn 隔离修 T_DEREF 污染）；⑤ 声明符括号 `int (*p)[3]`：pd() `(` 分支（变量按指针 slot、so_rp=1、吞 `[N]`）；全局路径 ptop_one `(` 分支（rpflag/grp，pos 回退到 `;` 供 pgv 收分号）；数组指针 `(*p)[i]`＝指针值作基址（so_rp/grp 探测 oq 顶 T_DEREF 弹出、`*p` 不再读内存）、后缀 `[i]` 分支（值强制 4 字节读）；⑥ 三维数组：局部（pd 三 `[N]`→so_p=跨度 so_p2=第三维、e_expr/p_a 栈式 `popl edi/edx/ecx` 两重 imull＋addl，p_a 写路径索引在栈上防 p_a() 递归破坏寄存器）；全局（gcol=跨度/gcol2=第三维）；`&` 三维暂记限制；局部 struct/数组初始化器暂记限制（boot0 无 copy-init）。**引擎上限族**：E8 pggcc4 MAX_STR 1024→4096（重登记量大）、E9 pggcc4 in_buf 65536→131072（boot0.pgc 66KB＋，截断引发字符串"未闭合"误报——gdb 断 str 未闭合分支实证）；boot0 自表 grp/gcol2/so_p2/so_par/so_pst/so_rp 新增 | 无任何编译器对照；期望值手算；ISO/IEC 9899:1990 §6.3.5/.6（`->`/`&`）、§6.5.2.1/2.2/2.3（结构体/数组/初始化）、§6.7.1/6.5.4.2（复合声明符・函数定义参数）、§6.3.3（指针与地址）；自研（§8.1 面扩张原则） | 标准+自研 | 2026-08-26 | **P4-II 剩余限制清零（链上）**：run_boot.sh **189→212 全绿不回归**（新增 23 例：匿名嵌套 2、struct 数组/初值/参数/局部数组 8、`&a[i]`/`&s.m` 3、声明符括号 1、`->` 4、三维数组 5）；boot_b4 闭台**全门 PASS**（固定点不破——门1/门2 曾因 p_s 块回收 `print_int((sc-bstk[bd])*4)` 在 boot0 自编译产物丢 `*4` 破裂，改"先存局部 n2 再 `n2*4`"后 boot0b.s==boot0c.s 逐字节恢复）；run.sh **72/72**；全链 as+ld 无任何 C/C++ 编译器、无源码阅读；详见 docs/logs/2026-08-26.md 会话 6。**残余登记限制**：`&` 三维下标、局部 struct/数组初始化器（含字符串初值）、struct 数组传参、后缀 `[i]` 写、struct 多重指针 `->` 链 |


**来源类型** 取值建议：
- 标准 —— 正式 ISO/IEC 标准文本
- 公开草案 —— Committee Draft / WD，未正式发布
- 规范文档 —— Rust Reference、Go Spec、C++ cppreference 等权威在线文档
- 通用教材 —— 编译原理教材（龙书等）中的通用算法
- 行为观察 —— 通过真实编译器**运行结果**的观察（不涉及源码）

---

## 标准文档参考表

| 语言/版本 | 权威标准文档 | 说明 |
|-----------|--------------|------|
| C90        | ISO/IEC 9899:1990        | C 首次标准化 |
| C99        | ISO/IEC 9899:1999        | VLA、复合字面量、可变参数宏等 |
| C11        | ISO/IEC 9899:2011        | \_Generic\、匿名结构体/联合体、\_Atomic\ |
| C17        | ISO/IEC 9899:2018        | 缺陷修复为主 |
| C23        | ISO/IEC 9899:2023        | \#embed\、\	ypeof\ 标准化等 |
| C++98      | ISO/IEC 14882:1998       | 类、模板、重载 |
| C++11      | ISO/IEC 14882:2011       | \uto\、lambda、右值引用 |
| C++14      | ISO/IEC 14882:2014       | 泛型 lambda |
| C++17      | ISO/IEC 14882:2017       | 结构化绑定、\if constexpr\ |
| C++20      | ISO/IEC 14882:2020       | 概念、协程、\<=>\ |
| C++23      | ISO/IEC 14882:2023       | modules 完善等 |

> 若某语言标准尚未公开全文，优先引用**官方委员会草案（WD/CD）**或社区公认的权威参考（如 \cppreference.com\、Rust Reference）。

---

## 语言特性现状登记（查证记录，仅特性名，未下载任何源码）

> 按用户约定：查证资料只记录**特性**，绝不下载/保存任何编译器源码。来源均为公开标准文档与权威参考（WG14/WG21、cppreference、编译器支持矩阵）。

### C —— 现行标准 ISO/IEC 9899:2024（C23，2024-10-31 发布，`__STDC_VERSION__`=202311L，下一版 C2y 预计 2026-2029）

语法层新增特性：`nullptr`/`nullptr_t`、`true`/`false`/`bool` 关键词、数字分隔符 `'`、`typeof`/`typeof_unqual`、对象定义 `auto` 类型推导、`_BitInt(N)` 位精确整数、`char8_t` 与 `u8` 前缀、单参 `static_assert`、属性 `[[deprecated]]/[[fallthrough]]/[[maybe_unused]]/[[nodiscard]]/[[noreturn]]/[[reproducible]]/[[unsequenced]]`、`#embed`、`= {}` 零初始化、去除 K&R 旧式定义等。

库层新增：`memset_explicit`、`memccpy`、`strdup`/`strndup`、`memalignment`、`<stdbit.h>`（`stdc_*`）、`gmtime_r`/`localtime_r` 等。

### C++ —— 现行 C++23（ISO/IEC 14882:2023 定稿）+ C++26（2026-03-28 推送）

C++23 语法层代表特性：`if consteval`、多维 `operator[]`、`#elifdef`/`#elifndef`、显式对象参数（deducing this）等。

C++26 语法层代表特性：反射（`^^`）、契约（`contract_assert`/`pre`/`post`）、用户自定义 `static_assert` 消息、注解、占位符变量、trivial 可重定位、`constexpr` 放置 new 等。

> 优先级排布（C 轨道先行）：C23/C++26 特性作为远期 roadmap 输入，v0 之前只按 C89 起步，贴近真实对比对象 GCC 2.95.3 的行为面。

---

## TODO / 待补充来源

- [x] v0 — 词法/表达式优先级参考章节已登记（§6.1 词法、§6.3 表达式、§6.3.5 除法语义），2026-08-25 无蛋重推完成
- [x] v1 — 声明/标识符/赋值语义参考章节已登记（§6.1 词法、§6.3 表达式、§6.5 声明、§6.6 语句），2026-08-25 无蛋 stage-1 完成
- [x] v2 — 函数定义/调用/return 参考章节已登记（§6.1 词法、§6.3 调用表达式、§6.7 函数定义、§6.6 return），2026-08-25 无蛋 stage-2 完成
- [x] v3 — 控制流/比较/块作用域参考章节已登记（§6.2.1 作用域、§6.3.8/§6.3.9 比较、§6.6.3-5 复合/选择/迭代语句），2026-08-25 无蛋 stage-3 完成（B0 硬门槛：图灵完备）
- [ ] ...

---

*本文件遵循 MIT 许可协议发布（随项目仓库）。*
