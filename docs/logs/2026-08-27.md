# 操作日志 2026-08-27

> 格式：`时间 | 操作 | 参考来源 | 结果`
> 本文件 append-only，按日期切分，随 commit 推送 GitHub，作为 AI 行为的公开流水账。

## 会话 1（B5 链上缺陷排查与修复 —— 用户外部评测矩阵反馈）

用户提交外部评测矩阵（左侧知识/代码描述、右侧实测状态），共 3 行：

1. 经典冒泡排序（嵌套 for + `if(a[j]>a[j+1])` + swap）→「排序完成倒序」
2. `struct` 链表（自引用指针）→ 编译失败 `error: 146 next`
3. `float` / `#include` → 按预期编译失败（方言不含，属正常）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-27 | **复现（三链全中）**：场景脚本 build/repro_issue.sh（build/ gitignored）——bub.pgc（冒泡）经 bin1(x86)/bin_amd/bin_arm 均输出 `2 1 4 3 5 0`（期望 `1 2 3 4 5`）；ll.pgc（自引用链表）经三链均 `error: 28 node` | 黑盒行为观察（本地运行自身编译器） | 复现成立；三链同因（前端共享） |
| 2026-08-27 | **缺陷一为「for 语句后的 tokenizer 状态失步」**：最小化矩阵（`for(...){ } print_str(...)` 编译失败 `error: N print_st`、`for 后 q=7 存入错误槽 −4(%ebp)`、`for 后 j+q 得 5 而非 8`）→ 加装临时调试版 ml()（se() 额外打印 L/N/S/T/P/J），实测失败态 `T=ID L=9 N=j`——**`nm[]` 仍持 for 增量表达式回放时的循环变量名，而 pos/tk/tp/tl 已恢复为后续语句 token**。**根因**：for 语句增量经「扫描+回放」二次解析（p_s T_FOR：body 解析后 `pos=ip; nx(); p_a()`），回放用 `nx()` 污染全局 `nm[]`，语句尾仅 `pos=bpos; tk=btk; tp=btp; tl=btl` 恢复，未恢复 `nm[]` → 后续任何按名查找（bfind/fnfind/fi/tdfind）作用于过期名：紧邻的调用语句 bfind 失败即报错、赋值语句 es_ 解析到旧名槽位写出错值。**修复**：`p_s` 增全局 `char nm_sv[17]`，for 语句 body 后快照 `nm[0..17)`、增量回放后还原（B5 修复一，boot0.pgc 文件头 B5-2 段 leave mark） | 黑盒最小复现矩阵＋临时调试版 se() 状态转储；tokenizer 状态机实现阅读 | 修复后：for 后调用/赋值/嵌套全部正确（ve/xf/yf 探针全绿，`for+print_str` 出 `X`、`for+q=7` 出 `X7`） |
| 2026-08-27 | **缺陷二为「下标索引表达式运算符冲掉外层待定运算符」**：冒泡仍错（`a[j]>a[j+1]` 编译成 `a[j]+a[j+1]`——if 条件处 x86 asm 为 `addl` 而非 `cmpl+setg`），grep 定位 27 处 `q=opn; opn=0;`（e_expr/p_a 数组下标/`&` 取址/嵌套槽位解析），随后用 `e_expr()` 解析索引表达式。**根因**：`opn=0` 只隔离**栈针**，嵌套表达式操作符仍写 `oq[opn]` 从 0 起——当外层有未定操作符（如 `a[j]>…` 的 `>` 在 oq[0]），索引里的 `j+1` 的 `+` 便写到 oq[0] 覆盖之；外层最终抽干 `oq[0]` 拿到被覆盖的 token → `>` 变 `+`。单侧（`a[j]>3`）正确、双侧下标（`a[j]>a[j+1]`）必然触发 → 冒泡 if 恒真无条件 swap → 原序列 D 序时偶然正确（掩蔽）。**修复（B5 修复二）**：27 处改「压哨兵 `oq[opn]=T_LPAREN; opn++; tbs=tbd; tbd=opn;` → 索引解析 → `opn=q; tbd=tbs;`」——索引操作符写在哨兵之上，e_expr 全部 pop/drain 在哨兵（LPAREN）处停住，外层待定操作符零接触（说明：T_LPAREN/tbd 边界为既有机制，本次仅复用不新增全局；先试过的 osv()/ors() 槽位搬移方案自编译失败已回退，本方案自举链零回归） | C90 §6.5.2.1 数组下标（索引表达式为完整表达式语义）；算子栈（调度场）不变量：嵌套 parse 不得破坏外层待定符号；黑盒 asm 取证（addl 而非 setg） | 修复后全部正确：bub `1 2 3 4 5`（t5=12345）、`if(a[j]>a[j+1])` qg5 出 22212、zg5 二次冒泡 31245 正确 |
| 2026-08-27 | **缺陷三为「struct 类型名登记截断（历史潜伏）＋自引用指针成员不支持」**：自引用 `struct node { …; struct node *next; }` 复现失败（ll 于第二处 `node` se()；`struct node{int v;}; struct node g;` 亦失败而 `struct N{…}` 全过——整套测试只用了单字母结构名掩盖此坑）。**根因 A（登记截断）**：ptop_one 结构名登记 `pos=sp; nx()` 重扫——`sp` 存的是**名字 token 的 END（pos）**而非起点 `tp`，重扫实际吐出 `{`（tl 被标点置 1），登记仅写入 `stnm[0]=首字母`（1 字母名 tl=1 恰巧合 → 全链只测单字母名从未暴露）；后续 `stfind(全名)` 必然失配。**修复 A**：登记改存 `stl=tl`、直接 `while(q<stl){ stnm[j*16+q]=nm[q] }`（names 由 ID token 一次性写入，`{` 标点不冲 nm，无需重扫）。**根因 B（自引用指针成员）**：pstm 成员 `struct node *next` 的 `struct node` 尚在定义中（表未登记）→ stfind 失败即报错；按 C90 语义指针成员指向未完成类型合法。**修复 B**：pstm 遇未登记类型名且与正在定义的结构同名 → `nsid=j`（自引用）；非指针值成员 `nsid==j` 特判报错（无限大小类型非法） | ISO/IEC 9899:1990 §6.5.2.1（结构体标记/自引用指针合法：指向未完成类型）、§6.1.2.1（作用域）：标签在定义结束即可用；黑盒最小矩阵（1/2/3 字母结构名对照） | 修复后：`struct node{…}; struct node g;`、局部/参数/成员/数组全通，自引用链表 ll 三链输出正确；run_boot 新增自引用/多字母/链表回归 5 例 |
| 2026-08-27 | **缺陷四为「成员读作 RHS 未支持」（链表必需路径）**：自引用修复后 ll 仍挂于 `p=p->next;`（pos 186）——最小化 `x=a.v;`（成员读赋给变量）即失败：p_a 的 T_DOT/T_ARROW 成员分支只处理“写”（链后强制 `tk!=T_ASSIGN{ se() }`），成员读一律报错；既有通过用例全靠 `return s.x`（走 e_expr 表达路径）或写路径，`x=s.m` 从未覆盖。**修复（B5 修复四）**：该分支 `if(tk!=T_ASSIGN)` 改“镜像写路径发装载并 return”（x86 `movl/ movzbl off(%edx),%eax; pushl`；amd64/arm64 同步），将 `s.m`/`p->next` 作为 RHS 值载入 | ISO/IEC 9899:1990 §6.3.2.3（成员访问值语义，读与写对称）；黑盒最小复现（rm 探针） | 修复后 rm1`x=a.v`=7、rm2 完整 `p->next` 链=6、ll 链表求和=3（三链） |
| 2026-08-27 | **回归门全绿**：run_boot **237/237**（原 232＋新 5 回归：双向冒泡、自引用链表、四节点链表、成员读 RHS、多字母类型名）、run.sh **72/72**、run_amd **42/42**、run_arm **42/42**（AMD64/AArch64 共享前端，修复自动生效）、boot_b4 闭台**全门 PASS**（门1 boot0b.s==boot0c.s 逐字节固定点不破）、boot_amd/boot_arm 闭台全门 PASS（ARM64 门1 selfarm.s==selfarm2.s 逐字节） | tests/run_boot.sh、boot_b4/boot_amd/boot_arm.sh 门控 | 六线全绿；三目标行为一致 |
| 2026-08-27 | **测试矩阵结论回填**：冒泡排序（嵌套 for＋变址比较＋swap）→ 三链输出 `1 2 3 4 5` ✅；`struct` 自引用链表 → 三链求和正确 ✅；`float`/`#include` → 按预期错误（方言不含，设计如此）✅ | 用户矩阵＋本地复证据 | 矩阵 3 行全部落地 |

> 会话 1 自查：①四个缺陷均由黑盒行为（错误位置/token/asm 差异）定位，未读任何 C/C++ 编译器源码 ✅；②修复均先在 build/ 最小探针验证、再入 boot0.pgc，回放路径（for 增量、运算符哨兵）与既有机制（标号栈/算子栈/tbd 边界）同构 ✅；③全量回归六线全绿＋三目标闭台固定点不破 ✅；④发现的历史坑（多字母结构名登记截断、成员读 RHS、for 后 tokenizer 失步、下标索引运算符冲撞）均已登记 ✅；⑤全链 as+ld 无任何 C/C++ 编译器参与、无源码阅读 ✅

**遗留登记（未在本次范围）**：`struct` 数组元素 `.m` / `p[i].m` 作 RHS 读（`x=arr[i].m`）仍走写分支报错——与本次修复的 `x=s.m` 同类但分支不同（数组+链组合），需链上扩充时一并处理；`float`/`double`/`#include` 等 C90 外特性按方言设计预期拒绝。

## 会话 2（LeetCode 经典题压测 —— 语言漏洞验证，用户要求）

构建 LeetCode 16 题运行矩阵 `build/lc_run.sh`（pgg 方言直写，期望值全手算，无任何外部编译器对照）：
two_sum / 回文数 / 整数反转 / 爬楼梯 / 二分查找 / 合并有序数组 / 有效括号 / 去重 / 最大子序和 / 链表反转(206) / 整数平方根 / 只出现一次 / 罗马数字 / 验证回文串 / 移动零 / 买卖股票。

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-27 | **初跑 16 题：PASS 11 / FAIL 5**。失败分离：① merge_sorted/rm_dups/move_zeroes 三题自写测试正文输出循环漏写 `i=i+1`（死循环刷屏）——测试代码错误非编译器缺陷；② sqrt_int 期望 46339 得 1073697799——`mid*mid` 32 位溢出（方言无 long/64 位整数，`x/mid` 防溢出改写后通过）——方言设计缺口；③ rev_list `error: 221 next`——真实编译器缺陷：**struct 数组元素 `.m` 的成员读写路径不支持指针成员** | 黑盒矩阵 | 分类完成：13 测试代码/设计缺口 + 1 编译器缺陷（rev_list） |
| 2026-08-27 | **rev_list 根因（B5 修复五，closures 三类分支）**：最小化（`nodes[1].next=0` 失败、`n.next=0` 成功、`nodes[1].v=7` 成功）定位 p_a 全局 struct 数组**索引写**路径（`g=gfd(); gn>0&&gstt>=0`），根因见 `if(ty==3){ se(); }`——与直接成员写路径的 `ty==3→op3=1; pt=mid` 指针链机制不一致（B5 修复四只做了直接路径）。**三处修复**：① p_a 索引写路径成员链改 op3/pt 指针链处理（链继续时装载指针值换基 %rdx、分段发码）；② e_expr 索引读路径（`nodes[i].next` 作表达式值）同样补 op3/pt；③ p_a 索引路径补 `if(tk!=T_ASSIGN)` 读分支（`x=nodes[i].m` 镜像写路径发装载——直接消灭会话 1 遗留登记项） | ISO/IEC 9899:1990 §6.5.2.1、§6.3.2.3；黑盒最小矩阵（下标/非下标/常量/变量/指针成员对照） | 修复后：链表反转 i386 正确 `54321`、三处最小探针 77/55/66 语义全对；amd64/arm64 共用前端修复自动生效 |
| 2026-08-27 | **新增发现并登记：AMD64/AArch64 后端成员存取宽度预存缺陷**（`rev_list` amd64 原生输出 `1` 暴露）：后端成员读写一律 8B（`movq (%rdx)`/`ldr x0,[x2]`），而 struct 布局按 4B 间距（`stsz=8` 的 node{v,next} 实际 4+4）→ 相邻成员/相邻数组元素**写重叠**；run_amd 42/42 与 boot_amd 门4 通过纯属「print_decimal 仅打印低 32 位＋数值巧合」掩蔽（`s.x=3,s.y=4` 实得 0x0000000400000003，低 32 位经运算巧合回 34）。**结论**：后端起按类型定宽（int=4B/char=1B/指针=8B）需统一改造十余处发射点＋arm64 同类，属独立后端子工程，本次不作为链上修复冒进 | 黑盒 asm 取证（p1.s：x/y 均 movq 8B、间距 4B）＋低 32 位打印机制阅读 | 已登记为待办；i386 主链不受影响（int=4B 布局一致，无重叠） |
| 2026-08-27 | **回归门全绿**：run_boot **241/241**（原 237＋新 4 回归：链表反转 54321、索引写指针成员、索引读链、索引读入指针变量）、lc 矩阵 **16/16**、run.sh 72/72、run_amd/run_arm 42/42、boot_b4 全门（重建后固定点不破）、boot_amd/boot_arm 全门；i386 固定点 boot0b.s==boot0c.s 逐字节 | tests/* + build/lc_run.sh | 六线全绿 |
| 2026-08-27 | **LeetCode 见证实测回填**：16/16 全过——两数之和、二分查找、爬楼梯（DP 数组）、链表反转（自引用 struct＋索引成员读写）、有效括号/罗马数字/回文串（char 数组/ASCII 运算）、只出现一次（^ 异或）、最大子序和/移动零/合并/去重/买卖（滑动窗口/归并/快慢指针）、sqrt（二分防溢出） | build/lc_run.sh | 语言漏洞验证完成：代表性题面覆盖数组下标、指针、结构体、字符串、位运算、递推全部通过 |

> 会话 2 自查：①rev_list 缺陷三处修复均黑盒最小探针定位（编译错误位置/token/成员表 dump），修复后先探针验证再入 boot0.pgc ✅；②sqrt_int 属方言设计缺口（无 64 位整数/无溢出保护），非编译器缺陷，防溢出改写通过 ✅；③AMD64/AArch64 成员宽度缺陷为预存后端问题，证因（8B 访问 vs 4B 布局）后登记不冒进，主链不受影响 ✅；④全链 as+ld 无 C/C++ 编译器参与 ✅