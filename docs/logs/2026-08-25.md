# 操作日志 2026-08-25

> 格式：`时间 | 操作 | 参考来源 | 结果`
> 本文件 append-only，按日期切分，随 commit 推送 GitHub，作为 AI 行为的公开流水账。

## 会话 1（实验重置确认 + 实验/环境基线建立）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-25 | 通读仓库全部文档：README / ORIGIN / plan-language-features / .gitignore / LICENSE / tests/run.sh；并从 git 历史读取已删的 architecture-v0.md 与 2026-08-24.md 日志，梳理环境底账与流程约定 | 仓库文档 + git 历史 | 完成 |
| 2026-08-25 | 用户确认对已删文件"确认删除、重置干净"，提交删除：src/pggcc.c、docs/design/architecture-v0.md、docs/logs/2026-08-24.md | 用户决策 | commit `84204c0`，工作区回净 |
| 2026-08-25 | WSL 工具链复核（**仅环境清点，未做任何对照/实验**）：gcc 15.2.0、as 2.46、ld 2.46；`/lib32/crt1.o` `crti.o` `crtn.o` `libc.so.6` 在位；g++ / flex / bison / makeinfo 均无；Ubuntu 家目录仅 .bashrc/.profile/.bash_logout，无残留；登记坑位：WSL 默认发行版为 docker-desktop（无 bash），命令须 `wsl -d Ubuntu` | 环境清点 | 与 2026-08-24 销毁审核后状态一致 |
| 2026-08-25 | 产出 `docs/design/experiment-baseline.md`：实验定义（被测对象/方法/阶段门控）、硬约束、环境基线（宿主/WSL 工具链/文档体系）、实验流程（自研 4.1 / 对照 4.2）、实现基线约定、当前状态、变更登记 | 用户指令："写好基本的实验和环境，看好全部文档，做出相应修改；（先别做实验）" | 已成文 |
| 2026-08-25 | 联动修改：README（08-25 重置记录、v0 状态=⏳待重推、基线文档链接替代已删 architecture-v0、构建节注记）；ORIGIN（v0 特性登记=待重推并注明首研/重置史）；tests/run.sh（头注重置与管线说明） | 基线与重置状态 | 已修改（随本日志提交） |

## 会话 2（"彻底无蛋"策略决策）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-25 | 讨论"蛋"的年代一致性：明确蛋只是聋哑翻译器（不提供语义），现代性污染三通道（构建诊断=顺向无害；oracle 诱惑=已被门控禁止；模型自身先验=靠标准+2.95.3 黑盒+ORIGIN 登记抵消）；年代锚点=标准+门控期 oracle+登记，非蛋的年份 | 用户质询"如何保证是那一年的零阶段" | 结论成文 |
| 2026-08-25 | 用户决策：**彻底无蛋**——编译器本体改用手写 i386 汇编（stage-0，无 libc/裸 syscall），构建链 as --32 → ld -m elf_i386（无 crt），全链不调用任何 C/C++ 编译器；gcc 15.2 从构建链退役（可卸载）；用户补充："会汇编也是理解编译器的实验之一" | 用户决策 | 策略确立 |
| 2026-08-25 | 基线文档新增 §6 无蛋引导策略（stage-0 形态/构建链/裸 syscall 自证/as·ld 定位/后续自举路线），联动更新 README（无蛋引导说明、语言行、构建命令改 as+ld、v0 行注记）、ORIGIN（v0 备注=无蛋重推）、tests/run.sh（构建与链接改无 crt/libc） | 无蛋决策 | 已修改（随本提交推送候选） |

## 会话 3（卸载 gcc + 无蛋 v0 架构评审）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-25 | 用户决策：gcc 15.2 **立即卸载**；下一步**先架构评审**再开工 | 用户决策 | 双项执行 |
| 2026-08-25 | **卸载 gcc 系编译器**（root：`wsl -d Ubuntu -u root`）：apt purge gcc / gcc-multilib / gcc-15 系 / cpp 系 / libc6-dev / libc6-dev-i386 / libc6-dev-x32 / libgcc-15-dev / gcc-arm-none-eabi + autoremove --purge；保留非编译器运行库 libgcc-s1 / gcc-16-base；`/lib32`（crt/libc）随 libc6-dev-i386 一并移除 | 用户决策"立即卸载" | 环境无任何 C/C++ 编译器；as/ld（binutils 2.46）在位 |
| 2026-08-25 | 卸载后冒烟验证（环境验证，非对照实验）：手写裸 syscall 程序（`_start` + `exit(42)`）经 `as --32` + `ld -m elf_i386`（**无 crt/libc**）直链 → 运行退出码 42 ✅ | 冒烟（与 08-24 exit(42) 同类验证） | **无蛋构建运行链成立**；后续注入坑位：`wsl` 命令参数含 `$`/双引号时经 PowerShell 传参会展开，改用仓库 `build/`（gitignored）放输入文件规避（已清理） |
| 2026-08-25 | **v0 无蛋架构评审产出** `docs/design/architecture-v0-eggfree.md`（Winston 视角）：目标与验收、权衡 T1-T5（AST 节点池 / 无 libc I-O 与最小诊断 / 生成程序内嵌 print+_start 模板 / idivl 截断天然对齐 C89 / as·ld 工具边界）、模块划分与寄存器约定、里程碑 M1-M4、风险缓解、验收门、开放问题 | 用户决策"先架构评审"；基线 §6；读者即 Winston | 评审已成文 |
| 2026-08-25 | 联动更新：experiment-baseline（§3.2 环境表 gcc 已卸载//lib32 移除、§7 状态、§8 变更登记）；README（无蛋引导注明 gcc 已卸载 + 评审链接） | 上两行结果 | 已修改（随本提交推送候选） |
| 2026-08-25 | 用户指示"先定开放问题，评完就停止"→ v0 架构评审**接口定标**：空白=空格/\t/\n/\r；语法错误=`error: <位置> <token>`（退出码 2）；参数错误退出码 1；汇编→stdout、错误→stderr；数字域 int32 钳制不报；实现时可推翻并留痕 | 用户决策 | docs/design/architecture-v0-eggfree.md §7，评审收尾 |
| 2026-08-25 | 用户要求 plan-language-features.md 细化到**不查资料即可明白下一步该实现什么**（"不能说等等，核心特性要明确"）→ 新增 §4 P0–P3 实现细则（每阶段明确：输入协议/词法 token 集/文法 EBNF/语义/代码生成要点/自证点）、§5 P4–P8 明确核心特性集；§0 补无蛋实现形态与输入协议演化（P0=argv 单表达式；P1 起 stdin 程序文本）；P0 状态改 ⏳待重推 | 用户指令 | 已写入 plan-language-features.md 并提交 |
| 2026-08-25 | README 增补：①「文档导航」入口表（experiment-baseline / plan-language-features / architecture-v0-eggfree / ORIGIN / docs/logs / tests.run.sh）；②底部「其他（AI 注意事项）」8 条：对照红线 / 不读源码 / 实现形态（无蛋汇编）/ 开工前必读（plan §4 + 评审 §7）/ 文档纪律（ORIGIN+日志，不主动 push）/ 工程规范（Rule of Three）/ 验证边界（禁止任何编译器行为比较，含历史输出当 oracle）/ 环境坑位（wsl -d Ubuntu、PowerShell 传参坑） | 用户指令"加入各文档链接引导 + 底部其他含 AI 注意事项" | 已写入 README 并提交 |
| 2026-08-25 | **路线图自洽性审计**（README↔plan↔基线↔ORIGIN↔评审）：结论=主线一致（P0–P8 序列 / C89→C23 / C++ backlog / 无蛋形态 / 门控规则），无陈旧声明（grep 校验 v0✅/已完成/15.2.0 均仅存于历史登记）；修正 4 处：①README v1–v3 状态"📋待定"→"📋细则已定"并指向 plan §4.N；②v3 目标补比较/块作用域；③补 v4（P4）行、"…"改 v5+（P5–P8）；④architecture M4 条款引用勘误（§6.3 数字/§6.4 表达式 → §6.3 表达式、字面量 §6.1） | 用户质询"路线图是否合理和其他文档自洽" | 已修改并提交 |

## 会话 4（v0 无蛋 stage-0 实现与自证 —— 开始实验）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-25 | 用户指示"开始实验"→ 实施 v0（P0）无蛋 stage-0：手写 `src/pggcc0.s`（约 430 行 i386 AT&T 汇编，单文件）：裸 syscall（`_start`/`write`/`exit`，无 libc/crt）；词法（token：NUM `+ - * / ( )`，空白=空格/\t/\n/\r，int32 越界钳制不报）；递归下降（expr→term→unary→primary，栈式发码）；诊断 `error: <位置> <token>`→stderr，退出码 0/2/1 三态 | plan §4.0（输入协议/词法/文法/语义/代码生成/自证）；architecture-v0-eggfree.md §3 模块划分与寄存器约定、§7 接口定标 | `src/pggcc0.s` 已成文 |
| 2026-08-25 | 自证 first-run 暴露 BUG-1：`app_dec` 用 `%ecx` 作循环计数，但循环内 `call app_char` 破坏 `%ecx`（app_char 用 ecx 读 out_len）→ 循环失控读越界 → SIGSEGV（gdb 定位 `0x080493a0 in app_dec`）。修复：计数换用 `%ebp`（跨 call 安全寄存器），源指针仍 `%esi`；并在文件头固化寄存器约定注释（app_* 破坏 ecx/edx，跨 call 保持值须放 esi/ebp/edi）。附带：安装 gdb（调试定位，工具类同 as/ld，非 C/C++ 编译器，保留以利后续排障） | gdb 17.1（apt 安装，2026-08-25） | 修复后崩溃消除 |
| 2026-08-25 | 自证 second-run 暴露 BUG-2：`1+2*3` 得 -5、`2*(3+4)` 得 0、`5+3*4-2` 得 -9。归因：`pending_op`（待发码算子）存单一内存槽，递归下降重入（内层 parse_term 的 `*` 覆盖外层 parse_expr 暂存的 `+`）导致外层发错算子（`+`→subl、`*`→idivl）。修复：算子改压栈（pushl/popl），随递归深度自然嵌套；删除多余 pending_op 槽 | 自研调试（手工追踪生成 .s 指令序） | 修复后 10/10 |
| 2026-08-25 | **M3 自证**：`bash tests/run.sh` → 10/10 PASS（四则/括号/一元负/左结合/截断除法，期望值手算，与任何编译器无关）；构建链 `as --32` → `ld -m elf_i386`（无 crt）全程无 C/C++ 编译器参与 | run.sh 基线管线 | **PASS=10 FAIL=0** |
| 2026-08-25 | **M3 边界冒烟**（临时脚本，验证后删除）：`-7/2=-3`、`7/-2=-3`、`-7/-2=3`（idivl 商向零截断=C89 整数除法）；int32 钳制 `2147483648→2147483647`、超长数钳制；多重一元负 `---------5=-5`；深括号；空白输入；退出码空串=2、尾算子=2、非法字符=1、双数字=2、无参数=1、正常=0 | §7 接口定标 | 全部符合 |
| 2026-08-25 | **M4 收尾登记**：ORIGIN 特性清单 v0 行更新（来源：ISO/IEC 9899:1990 §6.1 词法、§6.3 表达式、§6.3.5 除法语义；实现日期 2026-08-25；无蛋重推完成）；TODO 勾除 v0 项；本日志会话 4 留痕；临时诊断脚本/文件全部删除（build/ 内仅剩构建产物，gitignored）；gdb 安装登记于此 | ORIGIN 特性清单 + 日志格式 | 已登记（待 commit 推送） |

> v0 验收门（architecture §6）自查：①tests/run.sh 10 用例全 PASS、构建链无 C/C++ 编译器 ✅；②生成 `.s` 人工抽检（栈式求值、截断除法正确、行缓冲输出）✅；③ORIGIN v0 行更新 + 当日日志条目 ✅；④未越红线（无编译器对照、无源码阅读）✅

## 会话 5（v1 变量声明与赋值 无蛋 stage-1）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-25 | 用户指示"开始 v1 变量声明与赋值的开发"→ 实施 v1（P1）无蛋 stage-1：新增 `src/pggcc1.s`（约 680 行，由 pggcc0.s 扩展）：**输入协议改为 stdin 程序文本**（一次 read ≤4096B，argv 单表达式协议废弃）；词法新增 IDENT/关键字 `int`/`=`/`;`/`,`；文法增 decl/assign（右结合、链赋值），stmt 判定经 1-token 前瞻（peek/restore）；符号表（≤32 变量、名 ≤16B，未声明/重复声明/超长/表满 → exit 2）；末语句值打印（值语句在 %eax，无值语句清零打印 0） | plan §4.1；architecture §3 一致性（v1 扩展面=加 kind 与符号表） | `src/pggcc1.s` 已成文 |
| 2026-08-25 | **栈帧实现推翻细则并留痕**：细则 §4.1 说"函数序言 `subl $N,%esp` 一次性预留 4B×变量数"；单遍编译无法预知声明总数 → 改为**运行时逐声明 `subl $4,%esp`**（第 k 个声明变量槽 = -4(k+1)(%ebp)，语义等价；声明可出现在任意语句位置，顺序执行保证先分配后使用） | plan §4.1 允许"实现时可推翻本细则但须日志留痕" | 已留痕，自证通过 |
| 2026-08-25 | 自证暴露 BUG-1（位置 4781 幽灵错）：`error: 4781 a`。gdb 定位：`tok_start=0x0804c94d`（scratch_name 内部）→ 输入游标 `%esi` 被新增的 `copy_name`/`sym_find`/`declare_var`（复制段）破坏且不恢复，后续词法从 .bss 深处读数。修复：三处 helper 自存自取 `%esi`（v0 寄存器约定"app_* 破坏 ecx/edx"教训扩展为"所有内部 helper 须自存自取 esi"） | gdb 17.1（v0 会话 4 安装） | 修复后位置恢复正常 |
| 2026-08-25 | 自证暴露 BUG-2：链赋值 `a=b=c=7` 得 0。归因：`assign_tmp`（赋值目标槽偏移）存单一内存槽，右值递归重入覆盖（与 v0 的 pending_op 同款坑）→ 外层赋值写错槽。修复：目标槽偏移压栈（pushl/popl），绑定发码期借 `%edi` 跨 call 保持 | 自研调试（v0 同型 bug 归类） | 修复后 10/10 |
| 2026-08-25 | **自证**：`tests/run.sh` 更新为 v1 用例（8 成功 + 2 错误）→ **10/10 PASS**（声明+赋值+读回参与表达式、多变量、链赋值、声明夹语句、空程序=0、未声明=exit 2、重复声明=exit 2）；构建链 `as --32 + ld -m elf_i386` 无任何 C/C++ 编译器 | run.sh 基线管线 | **PASS=10 FAIL=0** |
| 2026-08-25 | **边界冒烟**（临时脚本，验证后删除）**18/18**：变量互赋值 `a=b`；标识符含数字/下划线/大写（`a1/_x/A`）；自引用 `a=a-3`；4 层链赋值；纯声明程序=0；值语句后接声明结尾=0（last_valued 语义）；16B 名恰好=OK / 17B=exit 2；缺分号/未声明读/关键字作变量名/33 变量超表=exit 2；非法字符=exit 1 | §7 接口定标 + §4.1 自证要点 | 全部符合 |
| 2026-08-25 | **收尾登记**：ORIGIN v1 行（§6.1 词法/§6.3 表达式/§6.5 声明/§6.6 语句，2026-08-25）；TODO v1 勾除；本日志会话 5；临时诊断清理（build/ 仅剩构建产物，gitignored） | ORIGIN + 日志格式 | 已登记（待 commit 推送） |

> v1 验收自查：①run.sh 10/10（无 C/C++ 编译器参与）✅；②生成 `.s` 人工抽检（栈帧/赋值/值打印自洽）✅；③ORIGIN v1 行 + 日志 ✅；④未越红线 ✅

## 会话 6（v2 函数定义与调用 无蛋 stage-2）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-25 | 用户指示"开始 v2 函数定义与调用的开发"→ 实施 v2（P2）无蛋 stage-2：新增 `src/pggcc2.s`（约 900 行，由 pggcc1.s 扩展）：**打印语义改为 main 返回值**（_start 调 f_main → print）；词法新增关键字 `return`、`{`/`}`；文法新增函数定义（参数表支持可选 `int` 类型前缀——细则 EBNF 原是裸 IDENT，为贴近 C90 声明语义兼收）、`return` 语句、函数调用（primary 内）；符号表新增函数表（≤16 函数 ≤16B 名；未定义调用/重定义/缺 main → exit 2）；每函数变量表重置（函数级作用域）：参数槽 +8+4k(%ebp)，局部沿用 v1 运行时逐声明 subl、-4(l+1)(%ebp)；cdecl 调用：实参从右到左压栈 + `call f_<name>` + `addl $4n,%esp` + 返回值 `pushl %eax` | plan §4.2；architecture §3 一致性 | `src/pggcc2.s` 已成文 |
| 2026-08-25 | **递归自证点顺延留痕**：细则 §4.2 自证要点含"递归（fact(5)==120）"，但递归需 base-case 条件分支——`if` 属 P3（控制流，流程依赖）→ 本阶段用嵌套/链式调用自证（如 `g(g(3))`），递归在 v3 引入 `if` 后补 | 计划依赖 | 已留痕 |
| 2026-08-25 | 自证调试修复 BUG-1（执行流跳进输入缓冲 SIGSEGV）：`peek_token` 原将 token 备份 push 到编译栈且函数 `ret` 在备份之上弹出——ret 弹出输入指针（备份值）→ 执行 in_buf 文本。修复：peek/restore/commit 改**内存深度栈**（pk_*+pk_depth，8 层，含下溢/溢出防御），不再触编辑栈（v1 单槽 save_* 的嵌套覆盖隐患一并解除） | gdb（readelf/nm 坐标） | 修复 |
| 2026-08-25 | 调试修复 BUG-2（多函数程序 `error: <pos> int`）：`parse_top` 用 `je parse_func`（跳转）调用函数定义——`parse_func` 的 `ret` 弹回主流程（call parse_top 的返回地址），首个函数解析完即跳出 func* 循环 → 结束检查缺 main 报错。修复：`call parse_func` + `jmp` 回循环（v1 parse_decl→jmp 循环同型教训） | 自研 | 修复 |
| 2026-08-25 | 调试修复 BUG-3（实参 `,` 被跳过）：`.Lpc_arg_loop` 在 `parse_assign`（已消费实参、token 停在分隔符）后再 `call next_token` 一次，把 `,` 跳过直达下一实参。修复：删多余 next（parse 函数"消费后 token 已读"约定，v1 词法约定一致） | 自研 | 修复 |
| 2026-08-25 | **cdecl 从右到左压栈实现（推翻"源码序即右到左"的草率初解）**：初版按源码序解析实参直接 pushl，`f(1,2,3)` 被调方读到 3,2,1 → 321。改为**扫描实参边界（按顶层逗号分隔、括号深度保护）→ 自右向左逐实参回放解析**（重设游标+next_token+parse_assign）→ 生成代码先压最右实参。调试中发现并修复连环栈/序问题（详见下列 BUG-4/5/6） | cdecl 约定（§4.2 语义"实参从右到左压栈"） | 修复后 `f(1,2,3)=123` |
| 2026-08-25 | 调试修复 BUG-4（`.Lpc_rev` 用 `addl $4,%esp` 弹"起点"，实弹栈顶计数槽 N）：栈序 [..][起点][N]，0(%esp)=N、起点在 4(%esp)——`addl $4` 弹掉 N，第二次循环把起点当 N 读到 0 起点 → next_token(esi=0) 崩。修复：起点列表改存内存 `arg_pos[]`，栈上仅双份 N（保留槽+计数槽） | gdb 打印循环迭代（N/start/esi/esp） | 修复 |
| 2026-08-25 | 调试修复 BUG-5（多参 `error: <pos> 3` 类）：回放（重设游标逐实参解析）会打乱输入游标，`.Lpc_emit` 尾部从错误位置读 token。修复：扫描记录顶层 `)` 位置 scan_end，收尾显式把游标定位到 `)` 之后再取下一个 token | 自研（生成 .s 抽检 + 报错位置对照） | 修复 |
| 2026-08-25 | 调试修复 BUG-6（嵌套调用 `g(g(3))` 的 `error: <pos> )`）：嵌套调用的扫描/回放覆盖外层 arg_pos/arg_count/scan_end。修复：回放改用**不可变调用栈**——扫描完成即把 scan_end、N、SENTINEL(-1)、起点列表全部 push 编译栈；循环 pop 起点（天然自右向左）、SENTINEL 判界；嵌套调用只在本层栈区之上压栈弹栈，不破坏外层栈序 | 自研（gdb 与反汇编对照） | 修复后全绿 |
| 2026-08-25 | **自证**：`tests/run.sh` 更新为 v2 用例（8 成功 + 3 错误）→ **11/11 PASS**（main 返回/多参数/嵌套调用/局部+参数/表达式/负参数；缺 main=2、未定义函数=2、重定义=2）；构建链 `as --32 + ld -m elf_i386` 无任何 C/C++ 编译器 | run.sh 管线 | **PASS=11 FAIL=0** |
| 2026-08-25 | **边界冒烟**（临时脚本，验证后删除）**18/18**：三层链式调用 f(g(h(8)))=31；多参数四则；负返回；参数/局部区分（a2）；空函数体=0；四参数；嵌套自实现 f(f(2))=42；函数名可被局部变量遮蔽（变量/函数表分离，C 语义）；参数重名=2；局部重名=2；超长函数名=2；重定义 main=2；空程序=2；return 后有语句仍执行（顺序流无跳转） | §4.2 自证要点 + §7 协议 | 全部符合 |
| 2026-08-25 | **收尾登记**：ORIGIN v2 行（§6.1 词法/§6.3 调用表达式/§6.7 函数定义/§6.6 return 语句，2026-08-25）；TODO v2 勾除；本日志会话 6；临时诊断清理 | ORIGIN + 日志格式 | 已登记（待 commit 推送） |

> v2 验收自查：①run.sh 11/11（无 C/C++ 编译器参与）✅；②生成 `.s` 人工抽检（cdecl 压栈序、call/addl/pushl eax、函数序言尾声、递归调用）✅；③ORIGIN v2 行 + 日志 ✅；④未越红线 ✅
> 注：v2 修复的 6 个自研 bug 中，BUG-1/4/6 均为"编译栈/内存共享状态被递归或嵌套破坏"家族（v0 pending_op、v1 assign_tmp 同族），已沉淀为寄存器/状态约定（文件头注释）：跨 call 保持值用 %esi/%edi/%ebp 或栈；递归重入状态一律压编译栈或内存深栈。

## 会话 7（自举路线固化；v3 控制流暂停）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-25 | 用户指示"先停止 v3 控制流开发" → 不开启 v3 实现，路线不动，仅固化自举方案 | 用户决策 | v3 保持 📋 细则已定（plan §4.3），未开工 |
| 2026-08-25 | 讨论"什么时候建议生蛋，然后逐渐自举"：①语义澄清——无蛋（不借外部 C/C++ 编译器）与生蛋（自产蛋：自研语言写自研编译器）不冲突，生蛋是无蛋路线的完成形态；②两道门槛——硬门槛=语言图灵完备（需 v3 控制流 + 递归自证 fact(5)==120）；实门槛=编译器本体设施（数组/指针/字符串/比较/逻辑）；③三时机对比（v3 后 / B0 决策点 / P8 后）→ **决策：v4 编译器核心子集完成后、P5 前生蛋**；④里程碑 B0–B6：B0=v3+递归自证（硬门槛）；B1=v4 核心子集（对标 subC）；B2=B0 编译器以 pggcc 语言重写；B3=手写汇编 stage 编 B0 源码→bin0；B4=煮蛋自举闭台验证（bin0 编 bin1、bin0 编 bin1 得 bin2 逐代比对）+ 跨链比对防 Thompson 后门；B5=切自举链日常化；B6=手写引导链退役、可复现重放 | 用户质询"什么时候建议生蛋，然后逐渐自举" | 方案商定 |
| 2026-08-25 | 固化路线：plan-language-features.md 新增 §6 自举路线（§6.1 两道门槛 / §6.2 生蛋时机决策 / §6.3 里程碑 B0–B6 + 推进次序提醒），§0 增补自举策略条目；README 文档导航 plan 行补注 §6；本日志留痕 | 用户确认"按这个方案把路线固化进文档和日志" | 已写入（随本 commit，未 push） |