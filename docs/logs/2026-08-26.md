# 操作日志 2026-08-26

> 格式：`时间 | 操作 | 参考来源 | 结果`
> 本文件 append-only，按日期切分，随 commit 推送 GitHub，作为 AI 行为的公开流水账。

## 会话 1（B4 煮蛋闭台验证 —— 用户放行执行 + pg_quiet 整改）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-26 | 用户放行执行 **B4 煮蛋闭台验证**（architecture-b2-bootstrap §8）→ 新增永久门脚本 `tests/boot_b4.sh`：链＝pggcc4 直出 `boot0.s`→bin0 → bin0 编译 boot0.pgc→`boot0b.s`→bin1 → bin1 编译 boot0.pgc→`boot0c.s`→bin2；门1 boot0b.s==boot0c.s、门2 bin1==bin2、门3 跨链 .s 比对、门4 三头行为矩阵、门5 pg_quiet 语义 | §8 门控定义；run_boot.sh 管线镜像 | 脚本成文，首轮 **门1/门2 FAIL、门3 异型、门4 24/24 PASS** |
| 2026-08-26 | **差异定位（1 字节）**：门1 差异仅 `14854a14855`——bin1 输出尾多 1 个空行（258838 vs 258837B）；bin0/bin1/bin2 各自两次运行完全确定性；strace 证实 bin1 末次 `write(1,"\n",1)` 多余 | gdb 式黑盒二分＋strace 6.19（write 序列比对） | 定为单字节尾换行 |
| 2026-08-26 | **根因确认**：boot0.pgc `eh()` 运行时模板对 `pg_quiet` 只门控 `call print_decimal`，尾部换行（`movl $10,%eax; call print_char`）无条件发射——pggcc4 编译的 bin0（自身 pg_quiet=1）静默退出 0 字节；bin0 自编译的 bin1（boot0 模板）无论目标是否 pg_quiet 恒打换行。复核：pggcc4 编译 `int pg_quiet=1; int main(){return 42;}` 目标 stdout=0 字节，bin0 编译同名目标 stdout=1 字节（`\n`）——违反 v4.1「跳过返回值的打印与换行」语义 | v4.1/B2-P0 S3 语义（pg_quiet 非 0 跳过打印与换行）；strace 末次 write 取证 | 模板缺陷坐实（boot0 侧，自研代码） |
| 2026-08-26 | **整改**：boot0.pgc `eh()` 尾换行改随 `print_decimal` 同条件（gpq<0 与 gpq>=0&&gpqv==0 两分支内）发射，exit 序列保持无条件；文件头 B4 find 留痕 | 自研（对齐 v4.1 语义）；嵌套 if 规避 ≥3 项 \|\| 链沉疴（会话 14 登记） | 修复后编译通过 |
| 2026-08-26 | **B4 复验（全门通过）**：门1 **PASS** boot0b.s==boot0c.s（259267B 逐字节一致）；门2 **PASS** bin1==bin2（编连二进制逐字节一致——门2 首轮 FAIL 系 **ld 在 .symtab 嵌入输入 .o 文件名符号**（boot0b.o/boot0c.o）所致，非机器码差异，脚本改同名 .o 路径重链消除后逐字节一致）；门3 异型（boot0.s 358352B vs boot0b.s 259267B，diff 23728 行）＝pggcc4 与 boot0 不同实现直出同源码的结构性差异，非逐字节可比，跨链语义等价由门4 矩阵＋run.sh/run_boot.sh 双侧验证覆盖；门4 **24/24** 三头同输出；门5 **3/3** pg_quiet 目标三头均 0 字节 | tests/boot_b4.sh（新） | **B4 煮蛋闭台验证通过** |
| 2026-08-26 | **双链回归**：run.sh **72/72**（pggcc4 链）、run_boot.sh **90/90**（bin0 链）——pg_quiet 整改不改非 pg_quiet 目标行为 | run.sh/run_boot.sh 管线 | 回归全绿 |
| 2026-08-26 | **留痕**：tests/boot_b4.sh 入库（永久门）；architecture-b2 §8 B4 改 ✅、B5 改「待用户放行启用」；ORIGIN 增 B4 行；本日志会话 1；build/ 临时调试脚本（dbg_*、t1/t2/d1-3.s/boot0d.s 等）删除，b4.log/run*.log 留存（gitignored 证据） | ORIGIN+日志格式 | 已登记（待 commit） |

> B4 验收自查（architecture-b2-bootstrap §8）：①门1 boot0b.s==boot0c.s 逐字节一致 ✅；②门2 bin1==bin2 二进制逐字节一致 ✅（ld 文件符号噪声已消除，真机器码固定点）；③跨链：pggcc4 直出/自举产链为不同实现同源码直出，语义等价以行为矩阵（bin0/bin1/bin2 同输入同输出 24/24）＋双链回归验证 ✅；④整改项 pg_quiet 语义回归（门5 三头 0 字节）✅；⑤全链 as+ld 无任何 C/C++ 编译器、无源码阅读 ✅
> 结论：**B4 煮蛋闭台达成**——boot0 自举链自 bin0 起即达固定点（bin0→bin1→bin2 逐字节收敛），可切 B5（待用户放行）。
>
> 附：B5 内容（§8）＝日常开发切到自举链（bin0 面随需在链上扩张），P5–P8 在链上继续；手工 stage（pggcc4 系）退役时点待 B5 执行中商定。

## 会话 2（B5 切自举链启用 —— 用户放行）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-26 | 用户放行 **B5 启用**→ 切链冒烟（build/b5_smoke.sh 临时脚本）：S1 bin1 直接自举 `bin1 < boot0.pgc → bin1x`（259267B .s）✅；S2 固定点复验 bin1x 自举输出 == boot0c.s 逐字节一致 ✅；S3 链上编译器编译运行演示程序（pg_quiet=1 + 递归 fact(5) + 全局数组 + print_str/print_int）输出 `b5:120:` ✅；S4 run_boot.sh **90/90** ✅ | architecture §8 B5 + §8.1 约定；自研 | 自举日常开发循环实证成立 |
| 2026-08-26 | **冒烟发现面缺口（登记）**：演示初版含 `int len(char *s)` 形参 → bin1 报 `error: 69 char`——**boot0 面不含 `char*` 形参**（B2/B2g 明确未用，闭合面，非缺陷）；演示改面内构造后通过。该缺口列入 B5 链上待扩张清单（§8.1 面扩张原则） | 黑盒行为观察（error 位置/token） | 已登记 |
| 2026-08-26 | **B5 启用文档化**：architecture-b2 §8 B5 改 ✅ 已启用（2026-08-26 用户放行）＋新增 **§8.1 B5 操作约定**（改源码＝改 boot0.pgc、构建链 bin1→as→ld、回归门 run_boot 90＋boot_b4 闭台复验、面扩张原则、run.sh 降级为快照回归；pggcc0–4.s 冻结为跨链参照，退役时点执行中商定）；README B2 行同步；本日志会话 2；build/b5_smoke.sh 删除 | architecture §8 承接定义 + 工程约定 | 已登记（待 commit） |

> B5 启用自查：①链上自举循环（bin1 直编自身→固定点不破→链上编译运行）实证 ✅；②面缺口（char* 形参）黑盒定位并登记，链上面扩张走 §8.1 原则 ✅；③文档（§8/§8.1/README/日志）同步 ✅；④全链无任何 C/C++ 编译器、无源码阅读 ✅

## 会话 3（B5 首轮链上面扩张 —— char*/int* 形参）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-26 | 用户选择 **面扩张演示轮** 为 B5 后首个开发目标 → boot0 补 `char*`/`int*` 形参：①全局增 `so_t[32]/so_p[32]` 形参类型表（pfun 解析 `int`/`char` 前缀＋可选 `*`，登记 so_t=0/1、so_p=0/1；pd() 局部登记 0/0）；②e_expr 与 p_a 增**指针形参下标读/写路径**（栈序：base→index→[v]，char* esz=1 movzbl/movb（%eax,%ecx）缩址，int* imull$4+addl）；③自用：新增 `void wpf(char *s, int v){ print_str(s); print_int(v); }`，替换 pfun `f%d:`/pgbss·pgvk `g%d`/sbro `str%d:` 的 print_str+print_int 对——字符串字面量实参路径自证（闭合由构造保证） | §8.1 面扩张原则（自研，无任何编译器对照；嵌套 if 规避 ≥3 项 \|\| 沉疴） | 实现完成 |
| 2026-08-26 | **链上发现 E5**：pggcc4 编译 boot0.pgc 报 `error: 27396 "`——gdb 断 `.Lns_str` 条数检查（`cmp $0x100,%ebp`）实证 **check#257 str_count=256**：boot0.pgc 136 个唯一字面量，×peek/回放重复词法 ~1.9× → 实入 257 次 > MAX_STR(256)（HEAD 127×≈2≈254 擦边）。**E5：MAX_STR 256→512**（登记 pggcc4.s 头；同 E2/E4 引擎上限族） | gdb + 单串/多串对照探针（实验A/B：长度路径报串首、条数路径报第 257 串） | E5 生效后 boot0 编译通过 |
| 2026-08-26 | **链上发现 fi_i**：首轮新用例 9/9 失败（error: <形参名>），最小复现 `s[0]` 报 `error: s`——定位 **fi() 返回 `so[i]` 槽偏移而非符号索引**，新下标路径拿「偏移」去索引 `so_p[]` 必然错位（plain-ID 旧路径把 fi 结果当偏移用 em_ld 故不受影响）→ 新增 `int fi_i()`（返回索引）供指针形参下标路径使用 | 黑盒最小复现矩阵（转发 A 过 / 下标 C1-C4 挂）+ 符号表实现阅读 | 修复后 100/100 |
| 2026-08-26 | **自证与双门回归**：run_boot.sh **100/100 PASS**（原 90 全继承＋新 10：`sl(string 字面量/全局 char 缓冲)` 长度 2 例、`sum2(int*,n)` 全局 int 数组求和、`cv(char*,char*)` 双指针、`sh(char*)` 转发内建、`seta(int*,i,v)`/`setc(char*,v)`/`put(char*,i)` 指针写、`f(char*)+f(char*)` 复合）、期望值手算，无任何编译器对照；boot_b4.sh **门1/门2/门4/门5 全 PASS**（闭台固定点在新面下不破）；run.sh **72/72**（pggcc4 MAX_STR 改动回归） | run_boot/boot_b4/run.sh 管线 | 三线全绿 |
| 2026-08-26 | **留痕**：boot0.pgc 文件头 B5-1 段（so_t/so_p、fi_i、wpf 自用、E5）；architecture §8 头部执行结果块（B5 已启用＋首轮面扩张）；ORIGIN 增 B5-1 行；本日志会话 3；build/ 调试脚本（探针/gdb/loc/cnt/scan/min 系列）全删 | ORIGIN+日志格式 | 已登记（待 commit） |

> B5-1 验收自查（§8.1 面扩张原则）：①新面（char*/int* 形参＋指针下标读写）先引入 boot0.pgc 自身（wpf 自用）后自证 ✅；②run_boot 100/100（老 90 不回归＋新 10 手算期望）✅；③闭台复验 boot_b4 全门 PASS（固定点不破）✅；④run.sh 72/72（主链快照）✅；⑤发现的引擎/符号表坑（E5 MAX_STR、fi 返回偏移）登记 ✅；⑥全链 as+ld 无任何 C/C++ 编译器、无源码阅读 ✅

## 会话 4（P4 二期分片 T1–T5 —— 链上按复杂度递增推进）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-26 | 用户选择 P4 二期"按语法复杂度递增全面铺开"→ 分 T1–T7 七片（每片过 run_boot＋boot_b4 双门） | plan §5 P4（C90 全量）清单 | 计划成文 |
| 2026-08-26 | **T1 const/volatile＋指针声明符**：`const`/`volatile` 关键字＋声明位吸收（ptop_one/pd/pfun/pgv）；声明符 `*` 级联（so_p 多级、新增 pdlv/gptr 全局指针级）；`*`+`[N]` 数组声明（指针元素 4B）；pd() 改 `pd(int bt)` 带基型 | 自研（无任何编译器对照） | run_boot 108/108、闭环 PASS |
| 2026-08-26 | **T2 三目 ?:＋逗号＋sizeof**：`?`/`:` 词法；三目分支解析保留 opn（新增 tn/tbd/tbs：tn 冒号深度、tbd 分支基深、tbs 嵌套保存；RPAREN 与尾部 pending-op 抽干容忍外层括号）；逗号表达式（ar==0 或括号内做序列算子，实参顶层逗号仍为分隔）；sizeof（类型/标识符/全局数组） | C90 §6.3.15/§6.3.16/§6.3.6(逗号)、§6.5.3.3/§6.5.3.4 语义口径 | 首轮 q4/q5 括号内三目错→tbd 修复；run_boot 122/122、闭环 PASS |
| 2026-08-26 | **T3 位运算＋% 取模**：`%`/`~`/`^`/`&`/`\|` 单字符、`<<`/`>>` 双字符词法；`&`/`\|` 改可并列（`&&`/`\|\|` 保留原 T_AND/T_OR）；pr() 升格为 C 全序（一元 5 > 乘除模 4 > 加减 3 > 移位 2 > 比较 1 > 位与 0 > 异或 -1 > 位或 -2 > && -3 > \|\| -4）；em_o 增 idivl%edx 余、andl/orl/xorl/notl、shll/shrl %cl | C90 §6.3.5/§6.3.7 语义 | 旧错误用例 `2%3` 转正；run_boot 139/139、run.sh 72/72、闭环 PASS |
| 2026-08-26 | **T4 switch/case/default**：`switch`/`case`/`default` 关键字；每 switch 临时槽（swt）+**预扫描 1 遍**收集 case 常量并分配标号→发"先比较链后按序 case 体"分派（未命中→default/jmp end）；case 体按扫描序打标号（fallthrough 自然落入下一 case 体）；break 走 lcs/lbl 栈；嵌套 switch 经 swd 深度化 swcv/swlb（扁平 256 索引）；case 支持 NUM/CHR/enum 常量 | C90 §6.6.4.2（switch 语句，允许 fallthrough） | 首轮"temp 分配插在值入栈后→pop 空槽"gdb 定位修复（先 pop 再 subl），次轮"case 经预扫描+按序体"重构；run_boot 150/150、闭环 PASS |
| 2026-08-26 | **T5 enum＋typedef**：`enum` 词法＋ptop_one 全局枚举（csn/csv 常量表＋cstfind，e_expr 主元查常量、switch case 支持枚举常量）；`typedef` 词法＋ptop_one 注册别名（tdn/tdb/tdp＋tdfind），接线 pd/pfun/pgv/ptop_one（typedef 名在声明位视同类型，含 `T *p` 指针，局部经 pd(bt)） | C90 §6.5.2.2（枚举）、§6.5.5/§6.7（typedef） | **E6**：pggcc4 MAX_GLB 128→256（boot0 全局 131——T_* 常量行 56 个命名常量也计全局，gdb 断 Lsyn_err 实证 ast 越界）；**E6b**：boot0 自编译越界（自身全局表 gst/gn/gptr 128→256、gan 2048→4096）——链上引擎上限随本体增长同步升级 | run_boot 160/160、run.sh 72/72、闭环全门 PASS |
| 2026-08-26 | **留痕**：boot0.pgc 文件头 P4-II 段（T1–T5 机制＋E6/E6b）；pggcc4.s E6 注释；本日志会话 4；build/ 调试脚本（t1-t5smoke/gtest/lim/loc/gdb 系列）全删 | ORIGIN+日志格式 | 已登记（待 commit） |

> P4-II T1–T5 验收自查：①每片新面 run_boot 用例手算期望、逐片全绿且不回归（108→122→139→150→160）✅；②闭台 boot_b4 每片全门 PASS（固定点不破）✅；③run.sh 72/72（pggcc4 主链快照，MAX_GLB/MAX_STR 改动回归）✅；④发现的引擎上限坑（E6/E6b MAX_GLB）登记 ✅；⑤全链 as+ld 无任何 C/C++ 编译器、无源码阅读 ✅
> 剩余 P4-II：**T6 struct/union**（成员布局/点访问/嵌套/整值赋值——类型系统大件，独立一轮）、**T7 多维数组＋完整复合声明符语义**（`a[0][0]`、`int **`/`int *a[3]` 深层语义）——已圈定范围，次轮接续。

## 会话 5（P4-II T6 struct/union ＋ T7 局部数组/多维语义 —— 续接）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-26 | **接上次的 T6 半成品**：boot0.pgc 编译失败（error: 4397 `;` → 定位为上个会话新结构体表计数器误用 `stn` 与既有字符串计数器重名、pggcc4 重复定义错误；改 `stt`）| 黑盒错误定位＋自研阅读 | 编译通过 |
| 2026-08-26 | **接续编译错误（`%`/三目）**：pstm 用 `off%4` 与 `(r==1?1:4)` 均非 pggcc4 子集运算（`%` 与 `?:`）；改写为整除对齐、`if/else` 分支 | pggcc4 子集面（自研） | 编译通过 |
| 2026-08-26 | **E7：字符串表上限**：编译报"打开引号处 Lsyn_err"——逐段二分定位 pgquiet 段字面量登记点；print_str 调用 165→265、wpf 峰值 ~1.9× 重登记冲过 MAX_STR(512)；pggcc4 MAX_STR 512→1024、boot0 自表 stp0 512→1024、sts 16384→32768、`stc>=512`→1024 | gdb/E5 同源登记法（自研） | E7 生效，boot0 编译通过 |
| 2026-08-26 | **T7 局部一/二维数组**：pd() 增 `[N][M]` 后缀（so_sz=总元素、so_p=列数）；基址=so[si]（最低槽）；i386 寻址模式 `so(%ebp,%ecx[,4])` 直接读/写（char esz=1/int esz=4）；数组名退化 leal；sizeof(局部数组) | C90 §6.5.2.1/§6.5.6 语义口径（自研） | 局部 1D/2D/char/函数传参/sizeof 全过 |
| 2026-08-26 | **T6 struct/union 全链**：ptop 声明（命名/匿名定义、`struct X v;` 变量声明）；pstm 布局（int 4 对齐、char=1、union=max、嵌套成员 smt=2+sms）；点访问读（e_expr）/写（p_a）支持 `s.a.b` 链（偏移累加、末尾成员定 char/int、movzbl/movl）；struct 全局（gstt 记录 sid、pgst 按 stsz .space）与局部（so_st=sid+1、按 ceil(stsz/4) 分配栈槽）；整值赋值 `s2=s1`（ecpy 编译期展开 movl/movb 拷贝，局部/全局）；sizeof(struct/union) | C90 §6.5.2.1/§6.5.2.2/§6.5.3.5 语义口径（自研） | 冒烟 27/28（匿名嵌套定义=登记限制）|
| 2026-08-26 | **修 3 处解析洞**：①点分支先 `nx()` 吃 `.` 后 `while(tk==T_DOT)` 永不进（改 while(1)+成员名判定）；②`pos=sp` 复位后少一 `nx()`（去复位、直接以保留 nm 解析——fi/gfd 依赖 nm）；③fi_i/gfd 按 tl 截断匹配、点分支 `nx()` 吃 `.` 后 tl=1 致 `g1` 只比首字母（点分支入口 `tl=stl` 恢复；T_ASSIGN 结构体判定提前到 restore 后） | 黑盒错误位置＋调试探针 print_int（自研） | **STAR 复用级发现：tl 在标点 token 后仍保留旧 ID 名但长度被污染——凡标点 token 后做名查找必须恢复 tl** |
| 2026-08-26 | **自证与双门回归**：run_boot **189/189**（原 167 不回归＋新 22：T6 12 例、T7 局部数组 9 例＋错误用例 1），期望值手算；boot_b4 闭台**全门 PASS**（门4 24/24 三头矩阵、门5 3/3 pg_quiet、门1/门2 固定点逐字节） | run_boot/boot_b4 管线 | 双线全绿 |
| 2026-08-26 | **留痕**：boot0.pgc 文件头 P4-II T6/T7 段＋E7；pggcc4.s E7 注释；本日志会话 5；ORIGIN 增 T6/T7 行；build/ 调试脚本（bis/dbg/cnt/pos/case 系列）全删 | ORIGIN+日志格式 | 已登记（待 commit） |

> P4-II T6/T7 验收自查：①新面（struct/union＋局部数组）先用 boot0 自编译＋冒烟手算期望、再入 run_boot（189/189 无回归）✅；②闭台 boot_b4 全门 PASS（固定点不破）✅；③E7（MAX_STR）与 E6 同族登记 ✅；④解析洞根因（tl 污染）登记为通用约束 ✅；⑤全链 as+ld 无任何 C/C++ 编译器、无源码阅读 ✅

## 会话 6（P4-II 剩余限制清零——匿名嵌套 struct / struct 数组·参数·初始化 / &a[i]·&s.m / 声明符括号 / 三维数组 / ->）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-26 | **T_ARROW 与 `->` 运算符**：token 表尾插 T_ARROW=58（T_STRUCT/T_UNION 顺延 59/60）；nx() `-`+`>` 双字符识别；e_expr/p_a 的 DOT/ARROW 统一分支（编译期成员链偏移、运行时指针值 movl 装载，局部 si/全局 g、so_pst/gstp 记指针目标 struct id，写路径 `p->m=v`） | 自研（无编译器对照）；C90 §6.3.5 | 篡 smoke：p->x 读/写/链/char 成员全过 |
| 2026-08-26 | **匿名嵌套 struct/union 定义**：pstm 遇 `{`（struct/union 后）递归建临时类型（stt++、stnm 空名、pstm(nsid) 递归布局），外层登记 smt=2+sms 链接；去重此前误加的 nx()（pstm(nsid) 尾部已吞 `}`） | C90 §6.5.2.1 | `struct { struct { int y; } s; } v;` 读写通过 |
| 2026-08-26 | **struct 数组/初始化/参数**：局部（p_s struct 分支按 stsz 缩放数组）+ 全局（pgv gstt+gn+按 stsz*an .space）；`a[i].m` 链读/写（e_expr/p_a 增 struct 数组分支：下标×stsz→leal 元素地址→成员链）；初始化器 pginit 递归平铺（dty 0=int/1=char/2+=struct，任意维 `{}` 展开、字段按 smt 出 .long/.byte，.data 段直排）；struct 参数＝引用传递（pfun 分支 so_par=1 且 so_pst 记 sid，调用侧自动 leal，函数体 `s.m` movl 装载；e_expr/p_a DOT 分支 prm 优先 so_pst） | C90 §6.5.2.1/§6.5.7（初始化）/§6.7.1 | 篡 smoke：全局数组初值/单 struct 初值/int·char 数组初值/struct 参数值语义全过 |
| 2026-08-26 | **&a[i] 与 &s.m**：T_BAND 取址分支重构——ID+`[e]` 下标（esz 按元素类型、1D/2D）、ID+`.` 成员链（leal base+off）、纯变量（原行为）；全局/局部双路径；**下标递归 e_expr 前必须 `q=opn; opn=0` 隔离**——否则递归收尾把外层 `*` 的 T_DEREF 弹掉（addr1 段错误根因，gdb 定位） | 自研；C90 §6.3.3 | 篡 smoke：`*&a[0]+*&s.m`、`p=&a[1]`、`p=&s.x` 全过 |
| 2026-08-26 | **声明符括号 `int (*p)[3]`**：pd() 遇 `(` 分支（`*`+ID 登记指针、so_rp=1、吞 `[N]`，1 slot）；全局 ptop_one `(` 分支（rpflag 传给 pgv→grp 表；**pos 回退到 `;`** 使 pgv 的 nx() 收分号——pgv 用「名字后 token」气息，括号分支名前是 `)` 会误收）；数组指针语义：`(*p)[i]` 的 `*` 不再读内存（so_rp/grp 探测 oq 顶 T_DEREF 弹出、`p` 值即行首地址）+ 新增 e_expr 后缀 `[i]` 分支（栈顶基址 + i*4 强制读）；后缀 `[i]` 的 aw 判定：aw==0（后缀位置）合法、aw==1 报错 | 自研；C90 §6.5.1（声明符）、§6.3.5 | 篡 smoke：`p=&a; return (*p)[1];`→10 通过 |
| 2026-08-26 | **三维数组**：局部 pd 三个 `[N]`（so_p=第一维跨度 D1*D2、so_p2=第三维 D2、so_sz=总积）；全局 pgv 同理（gcol=跨度、gcol2=第三维）；e_expr/p_a 四分支栈式扩展（`popl %edi/%edx/%ecx` 两重 `imull`+`addl`，%edi 承运第三维）；**p_a 写路径索引保持在栈上、赋值后再弹**（先弹索引进寄存器再递归 p_a() 会因 p_a 内部 clobber %ecx/%edx 丢索引——T7 回归 `a[1][2]+a[0][2]` 期望14得12 根因） | 自研；C90 §6.5.2.1 | 篡 smoke：局部/全局 3D、`a[1][1][1]` 读写、char 3D、init 全过 |
| 2026-08-26 | **E8/E9 引擎上限**：E8 pggcc4 MAX_STR 1024→4096（本面 print_str 调用大增后重登记量逼近）；E9 pggcc4 in_buf 65536→131072——**boot0.pgc 达 66KB+ 超 in_buf 截断**：词法读缓冲末尾之外 → `.Lns_str` 遇 `\0` 报"未闭合"、err 位置诡异（65441）且与源码字符串 191B 全合法矛盾；gdb 断 `.Lns_str` 未闭合分支（edx 打标 2222）+ `%esi` 指向 bss 区实证 → in_buf 扩容修复 | gdb/tag 分段（自研）；E 系列先例 | boot0 编译通过 |
| 2026-08-26 | **自证与双门回归**：run_boot **212/212**（原 189 不回归 + 新增 23：匿名嵌套 2、struct 数组/初值/参数/局部数组 8、&a[i]/&s.m 3、声明符括号 1、-> 4、三维数组 5），期望值手算；boot_b4 闭台**全门 PASS**（固定点不破）；run.sh **72/72** | run_boot/boot_b4 管线 | 双线全绿 |
| 2026-08-26 | **留痕**：boot0.pgc 文件头 P4-II 余项段（机制 + E8/E9）；pggcc4.s E8/E9 注释；本日志会话 6；ORIGIN 增 P4-II（余项）行；build/ 调试脚本全清 | ORIGIN+日志格式 | 已登记（待 commit） |

> P4-II 余项验收自查：① 六项限制（匿名嵌套、struct 数组/参数/初始化、&a[i]/&s.m、声明符括号、三维、->）逐一新增用例、期望值手算、212/212 无回归 ✓；② 闭台 boot_b4 全门 PASS（固定点不破）✓；③ E8/E9（MAX_STR/in_buf）成因登记（gdb 实证未闭合分支与截断）✓；④ 解析洞根因登记（`&` 下标 opn 隔离防 T_DEREF 污染、p_a 写路径索引栈上防寄存器破坏、全局括号声明 pos 回退到 `;`）✓；⑤ 全链 as+ld 无任何 C/C++ 编译器、无源码阅读 ✓。
> **残余登记限制**：`&` 三维下标、局部 struct/数组初始化器（含字符串初值）、struct 数组传参、后缀 `[i]` 写、struct 多重指针 `->` 链。


> **B4 门1/门2 固定点复验补充**：新面展开后首次自举固定点破裂——boot0b.s != boot0c.s（diff 16 行/4 处 `addl $X`），bin1 给出非 4 倍数回收（$1/$2/$5）。经 `addl $X` 对齐性统计（boot0c.s 独有非 4 倍数）＋最小复现（bin0 产 `addl $4` vs bin1 产 `addl $1`，块内 `struct P arr[2]`）确认根因：**p_s 块回收 `print_int((sc-bstk[bd])*4)`——括号内下标减法再乘常数——在 boot0 自编译产物中被错译（丢 `*4`）**；pggcc4 直出 boot0.s 无此问题（门3 异型成立）。修复：改「先存局部 `n2=sc-bstk[bd]` 再 `print_int(n2*4)`」，固定点恢复（**门1 boot0b.s==boot0c.s 逐字节、门2 bin1==bin2 逐字节**，门4 24/24、门5 3/3）。登记通用约束：**避免 `(局部-数组[下标])*const` 复合表达式位于自编译热路径**（boot0.pgc 文件头留痕）。

## 会话 7（文档与代码实际对账＋过时表述同步）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-26 | **三线回归实测**：run.sh 72/72、run_boot.sh 212/212、boot_b4.sh 全门通过（门1 boot0b.s==boot0c.s 599909B 逐字节；门2 bin1==bin2 二进制；门4 三头矩阵 24/24；门5 pg_quiet 3/3；门3 跨链异型＝pggcc4 直出 871007B vs bin0 直出 599909B），自举稳定性确认；首轮 run_boot 200 FAIL 系 /tmp 遗留 root 属主 b0_case.o（`Permission denied`），sudo 清理后全绿（环境问题非代码） | tests/run.sh、run_boot.sh、boot_b4.sh 管线 | 三条自证链全绿 |
| 2026-08-26 | **文档对账**：ORIGIN 与日志登记与源码一致（pggcc4.s MAX_FUNC 64/MAX_GLB 256/MAX_STR 4096/in_buf 131072、boot0.pgc T_ARROW/so_par/so_pst/so_rp/so_p2/grp/gcol2/E8·E9/n2 修复均在位）；同步过时表述：README（run_boot 90→212、T6/T7「待续」→T1–T7＋余项完成、160 门→212、B4 259267B→最新一轮 599909B）、architecture-b2 §8/§8.1（回归门 90→212）＋头部执行结果块补 P4-II 增量行、plan P4 行 📋→✅ 已落地、experiment-baseline §7 改「状态快照（2026-08-25）」防误读为当前态 | 实测结果（黑盒运行，无任何编译器对照） | 4 文档同步完成（commit + push GitHub） |

## 会话 8（P4-II 收口 —— 5 项残余限制清零，C90 全量达成）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-26 | **& 1D/2D/3D 下标取址（局部+全局）**：T_BAND 取址多维化——镜像读路径（e_expr 441-464/505-525）统一平铺索引（`pushl %ecx` 还原首维、`popl %edi/%edx/%ecx` 两重 `imull`＋`addl`，2D：`i*so_p+j`、3D：`i*so_p+j*so_p2+k`；so_p=跨度 D2*D3、so_p2=第三维；全局 gcol/gcol2 同理）；struct 数组/指针数组只 1 维（so_st/gstt 门） | C90 §6.3.3（&）/§6.5.2.1（下标）；读路径先例（自研） | 局部/全局 1D·2D·3D `*&a[i][j][k]` 全过 |
| 2026-08-26 | **定位并修复全局 & 的 tl 污染 bug**：`&ga[1]` 报 `error: <pos> [`——共享首维解析（`nx();nx();e_expr()`）后标点把 tl 置 1，全局 else 分支**二次 `gfd()` 按 1 字符查「ga」必失败**（e_expr 局部用索引、p_a 全局写 gfd 在标点前所以未踩）；探针实证 `|GFD-1|`；修复＝else 分支复用入口已解析的 g，不再二次查名（登记通用约束延续：标点后按名查找须恢复 tl 或复用前期解析） | 黑盒探针（print_str/print_int 标记塞入 .s）＋源码阅读 | 修复后全局 & 全绿 |
| 2026-08-26 | **局部初始化器**：pd() 增标量（`ar+1` 包裹停声明逗号——否则逗号被当逗号表达式吞掉）、一维 `{..}`（int `movl $v, off(%ebp)`/char `movb` 平铺）、char 数组字符串（`sts` 字节常量逐写 `movb $k`）；p_s struct 分支增 `struct s={f..}` 与 struct 数组平铺（`o3` 分步 `e3*stsz[j]`+`smo` 防 pggcc4 复合表达式沉疴；嵌套 `{}` 暂不支持 se） | C90 §6.5.7（初始化）；§8.1 面扩张原则 | 标量/数组/字符串/struct/struct 数组全过 |
| 2026-08-26 | **struct 数组传参**：元素 by-ref（e_expr struct 数组元素分支本就推地址，t10/t11 36/56 直接过）；补 **struct 指针下标 `p[i].m` 读写**（e_expr/p_a 新分支 so_pst>0&&so_p==1：`em_ld` 装指针值→索引×stsz→成员链；原 d==1 只按 int 缩 4）——`f(ga)` 整组数组按指针、`p[1].x=v` 写入全通 | C90 §6.5.2.1/§6.7.1 | 99/7/9 等用例通过 |
| 2026-08-26 | **后缀 [i] 写**：p_a 增 `(` 分支（`(*p)[e]=v`：第二 token 非 `*` 回退表达式解析防 RHS 括号表达式回归——`a=(1,2,3)` 误入分支曾致 4 例回归）；基址 movl 指针值、索引×4、movl 写；局部 so_rp/全局 grp 双路径 | C90 §6.3.6（下标赋值）；自研 | `(*p)[i]=v` 局部/全局通过 |
| 2026-08-26 | **struct 多重指针 -> 链**：pstm 增指针成员（`struct T *name`→smt=3/sms，4B 布局）；链遍历（e_expr 读/p_a 写）改**延迟跳转**——op3/pt 标记指针成员，确认 `->`/`.` 后接成员才 `addl off`+`movl (%reg),%reg` 装载换基（链终点即指针值本身，`m.n=&a` 不再误跳转——首版立即跳转致 segv）；8 处变体链（数组元素/`p[i]` 等）对 smt=3 加 se() 守卫登记（多重链限主变量形态） | C90 §6.3.5（->）；自研 | `m.n->v` 读/写/偏置/全局全过 |
| 2026-08-26 | **固化与双门回归**：run_boot.sh 新增 20 例（& 3 例、后缀 [i] 写 2、struct 数组传参 4、局部初始化器 7、多重 -> 链 4）→ **232/232 全绿不回归**；boot_b4 闭台**全门 PASS**（门1/门2 固定点逐字节；门4 24/24；门5 3/3）；run.sh **72/72** | run_boot/boot_b4/run.sh 管线 | 双线全绿 |
| 2026-08-26 | **留痕**：boot0.pgc 文件头 P4-II 收口段；ORIGIN P4-II 余项行补收口（残余降为：局部嵌套 `{}` 初始化器、`&` 结构体链、struct 指针整体赋值等）；本日志会话 8；build/ 调试脚本与 t*.pg 冒烟全清 | ORIGIN+日志格式 | 已登记（待 commit） |

> P4-II 收口验收自查：① 5 项残余限制逐一新增用例、期望值手算、232/232 无回归 ✓；② 闭台 boot_b4 全门 PASS（固定点不破）✓；③ 发现的根因（全局 & 二次 gfd 的 tl 污染、RHS 括号表达式误入 LPAREN 分支、指针成员链终点误跳转）均黑盒定位并登记 ✓；④ 通用约束延续（标点后按名查找须恢复 tl / 复用前期解析）✓；⑤ 全链 as+ld 无任何 C/C++ 编译器、无源码阅读 ✓。
> **里程碑：P4（C90 全量）达成**——除登记残余（局部嵌套 `{}` 初始化器、`&` 结构体链、struct 指针整体赋值）外，链上已覆盖 C90 期面。

## 会话 9（基线复核＋P0 移植决策定稿入库＋GitHub 令牌启用与阶段性推送）

| 时间 | 操作 | 参考来源 | 结果 |
|------|------|----------|------|
| 2026-08-26 | **基线复核**：WSL Ubuntu 三线回归——run.sh **72/72**、run_boot.sh **232/232**、boot_b4.sh 闭台**全门 PASS**（门4 三头矩阵 24/24、门5 pg_quiet 3/3） | tests/ 管线（黑盒自证） | 基线全绿，自举固定点未破 |
| 2026-08-26 | **P0 移植决策文档入库**：`docs/design/platform-portability.md`（先前已定稿未跟踪）加入版本库；README 导航表补行、头部提示补 2026-08-26 移植决策段、文档纪律段"不主动 push"改为"阶段推送（用户 2026-08-26 要求每阶段上传 GitHub）" | platform-portability.md §6（Q1–Q5 拍板）；README 既有结构 | 文档同步完成 |
| 2026-08-26 | **GitHub 令牌启用**：读取 `D:\API_KEY\github令牌\classic-token.txt`（classic PAT），`git ls-remote` 验证鉴权成功（远端 HEAD = 本地 be50dc0，origin/main 已同步） | 用户提供令牌路径 | 令牌可用，推送通道打通 |

> 会话 9 自查：① 基线三线全绿（复核无回归）✓；② P0 决策文档入库 + README 同步 ✓；③ 令牌鉴权成功，后续每阶段 commit 后即 push ✓；④ 全链 as+ld 无任何 C/C++ 编译器、无源码阅读 ✓。
